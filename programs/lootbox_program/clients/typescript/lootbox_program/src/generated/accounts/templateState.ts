/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixZeroPodEncoderSize, getZeroPodDiscriminatorDecoder } from "../zeropodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findTemplatePda, type TemplateSeeds } from '../pdas';

export const TEMPLATE_STATE_DISCRIMINATOR = 4;

export function getTemplateStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(TEMPLATE_STATE_DISCRIMINATOR); }

/** Immutable template terms and the live finite inventory. */
export type TemplateState = { discriminator: number; authority: Address; boxMint: Address; oracleProgram: Address; oracleQueue: Address; id: bigint; opensAt: bigint;
/** Total bundle tickets ever activated. This is the lifetime issuance cap. */
totalBundles: bigint; totalMinted: bigint; remainingBundles: bigint; pendingOpenings: bigint; nextRequest: bigint; nextAllocation: bigint;
/** Increments after every activated append; snapshotted by each opening. */
version: bigint;
/** Undrawn inventory per append-only bundle, encoded as little-endian u64s. */
remaining: ReadonlyUint8Array;
/** Null-padded UTF-8 display name; never used for authorization. */
name: ReadonlyUint8Array;
/** Null-padded UTF-8 metadata URI; terms on chain remain authoritative. */
uri: ReadonlyUint8Array; bundleCount: number;
/** 0 draft, 1 live, 2 retired. */
status: number; bump: number;  };

export type TemplateStateArgs = { authority: Address; boxMint: Address; oracleProgram: Address; oracleQueue: Address; id: number | bigint; opensAt: number | bigint;
/** Total bundle tickets ever activated. This is the lifetime issuance cap. */
totalBundles: number | bigint; totalMinted: number | bigint; remainingBundles: number | bigint; pendingOpenings: number | bigint; nextRequest: number | bigint; nextAllocation: number | bigint;
/** Increments after every activated append; snapshotted by each opening. */
version: number | bigint;
/** Undrawn inventory per append-only bundle, encoded as little-endian u64s. */
remaining: ReadonlyUint8Array;
/** Null-padded UTF-8 display name; never used for authorization. */
name: ReadonlyUint8Array;
/** Null-padded UTF-8 metadata URI; terms on chain remain authoritative. */
uri: ReadonlyUint8Array; bundleCount: number;
/** 0 draft, 1 live, 2 retired. */
status: number; bump: number;  };

/** Gets the encoder for {@link TemplateStateArgs} account data. */
export function getTemplateStateEncoder(): FixedSizeEncoder<TemplateStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['authority', getAddressEncoder()], ['boxMint', getAddressEncoder()], ['oracleProgram', getAddressEncoder()], ['oracleQueue', getAddressEncoder()], ['id', getU64Encoder()], ['opensAt', getI64Encoder()], ['totalBundles', getU64Encoder()], ['totalMinted', getU64Encoder()], ['remainingBundles', getU64Encoder()], ['pendingOpenings', getU64Encoder()], ['nextRequest', getU64Encoder()], ['nextAllocation', getU64Encoder()], ['version', getU64Encoder()], ['remaining', fixZeroPodEncoderSize(getBytesEncoder(), 2048)], ['name', fixZeroPodEncoderSize(getBytesEncoder(), 32)], ['uri', fixZeroPodEncoderSize(getBytesEncoder(), 200)], ['bundleCount', getU32Encoder()], ['status', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 4 }));
}

/** Gets the decoder for {@link TemplateState} account data. */
export function getTemplateStateDecoder(): FixedSizeDecoder<TemplateState> {
    return getStructDecoder([['discriminator', getZeroPodDiscriminatorDecoder(TEMPLATE_STATE_DISCRIMINATOR, getU8Decoder())], ['authority', getAddressDecoder()], ['boxMint', getAddressDecoder()], ['oracleProgram', getAddressDecoder()], ['oracleQueue', getAddressDecoder()], ['id', getU64Decoder()], ['opensAt', getI64Decoder()], ['totalBundles', getU64Decoder()], ['totalMinted', getU64Decoder()], ['remainingBundles', getU64Decoder()], ['pendingOpenings', getU64Decoder()], ['nextRequest', getU64Decoder()], ['nextAllocation', getU64Decoder()], ['version', getU64Decoder()], ['remaining', fixDecoderSize(getBytesDecoder(), 2048)], ['name', fixDecoderSize(getBytesDecoder(), 32)], ['uri', fixDecoderSize(getBytesDecoder(), 200)], ['bundleCount', getU32Decoder()], ['status', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link TemplateState} account data. */
export function getTemplateStateCodec(): FixedSizeCodec<TemplateStateArgs, TemplateState> {
    return combineCodec(getTemplateStateEncoder(), getTemplateStateDecoder());
}

export function decodeTemplateState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<TemplateState, TAddress>;
export function decodeTemplateState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<TemplateState, TAddress>;
export function decodeTemplateState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<TemplateState, TAddress> | MaybeAccount<TemplateState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getTemplateStateDecoder());
}

export async function fetchTemplateState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TemplateState, TAddress>> {
  const maybeAccount = await fetchMaybeTemplateState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTemplateState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TemplateState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTemplateState(maybeAccount);
}

export async function fetchAllTemplateState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TemplateState>[]> {
  const maybeAccounts = await fetchAllMaybeTemplateState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTemplateState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TemplateState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTemplateState(maybeAccount));
}

export async function fetchTemplateStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TemplateSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<TemplateState>> {
  const maybeAccount = await fetchMaybeTemplateStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTemplateStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TemplateSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<TemplateState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findTemplatePda(seeds, { programAddress });
  return await fetchMaybeTemplateState(rpc, address, fetchConfig);
}