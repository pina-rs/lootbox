/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixPinaPodEncoderSize, getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findExclusiveSeriesPda, type ExclusiveSeriesSeeds } from '../pdas';

export const EXCLUSIVE_SERIES_STATE_DISCRIMINATOR = 10;

export function getExclusiveSeriesStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_SERIES_STATE_DISCRIMINATOR); }

export const EXCLUSIVE_SERIES_STATE_DISCRIMINATOR2 = 0;

export function getExclusiveSeriesStateDiscriminator2Bytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_SERIES_STATE_DISCRIMINATOR2); }

/**
 * Immutable rarity rules, art references, bonus reserves, and mint counters of
 * one template's Exclusive Lootbox NFT series.
 *
 * The PDA is the Bubblegum tree creator and the Core collection update
 * authority. Its lamports hold rent plus every unpaid tier bonus:
 * `lamports >= rent_reserve + sum(bonus_remaining[k] * bonus_lamports[k])`.
 * A separate zero-data fee vault PDA prepays Bubblegum's per-mint fee, so a
 * claim costs its submitter only the transaction fee.
 */
export type ExclusiveSeriesState = { discriminator: number; migrationVersion: number; authority: Address; template: Address; bundle: Address;
/** Metaplex Core collection; zero until the series is initialized. */
collection: Address;
/** Bubblegum V2 tree; zero until the series is initialized. */
merkleTree: Address;
/** Consolation copies the bound bundle slot can ever mint. */
quantity: bigint;
/** Editions minted so far; the next serial is `minted + 1`. */
minted: bigint;
/** Rent-exempt balance kept apart from the bonus reserves. */
rentReserve: bigint;
/** Bubblegum mint fee escrowed per copy in the fee vault at creation. */
mintFeeLamports: bigint;
/** Sixteen little-endian `u32` tier weights. */
weights: ReadonlyUint8Array;
/** Sixteen little-endian `u64` lamport bonuses paid per tier win. */
bonusLamports: ReadonlyUint8Array;
/** Sixteen little-endian `u32` bonus counts funded at creation. */
bonusCounts: ReadonlyUint8Array;
/** Sixteen little-endian `u32` bonuses still escrowed. */
bonusRemaining: ReadonlyUint8Array;
/** Sixteen little-endian `u32` editions minted per tier. */
tierMinted: ReadonlyUint8Array;
/** Null-padded UTF-8 name prefix; minted names are `{prefix} #{serial}`. */
namePrefix: ReadonlyUint8Array;
/** Null-padded UTF-8 symbol. */
symbol: ReadonlyUint8Array;
/** Null-padded `https://` base of every metadata URI. */
baseUri: ReadonlyUint8Array; maxBufferSize: number; assetIndex: number; contentsCount: number; backgroundCount: number; patternCount: number; maxDepth: number;
/** 0 configured (reserves escrowed), 1 ready (collection and tree bound). */
status: number; bump: number; feeVaultBump: number;  };

export type ExclusiveSeriesStateArgs = { authority: Address; template: Address; bundle: Address;
/** Metaplex Core collection; zero until the series is initialized. */
collection: Address;
/** Bubblegum V2 tree; zero until the series is initialized. */
merkleTree: Address;
/** Consolation copies the bound bundle slot can ever mint. */
quantity: number | bigint;
/** Editions minted so far; the next serial is `minted + 1`. */
minted: number | bigint;
/** Rent-exempt balance kept apart from the bonus reserves. */
rentReserve: number | bigint;
/** Bubblegum mint fee escrowed per copy in the fee vault at creation. */
mintFeeLamports: number | bigint;
/** Sixteen little-endian `u32` tier weights. */
weights: ReadonlyUint8Array;
/** Sixteen little-endian `u64` lamport bonuses paid per tier win. */
bonusLamports: ReadonlyUint8Array;
/** Sixteen little-endian `u32` bonus counts funded at creation. */
bonusCounts: ReadonlyUint8Array;
/** Sixteen little-endian `u32` bonuses still escrowed. */
bonusRemaining: ReadonlyUint8Array;
/** Sixteen little-endian `u32` editions minted per tier. */
tierMinted: ReadonlyUint8Array;
/** Null-padded UTF-8 name prefix; minted names are `{prefix} #{serial}`. */
namePrefix: ReadonlyUint8Array;
/** Null-padded UTF-8 symbol. */
symbol: ReadonlyUint8Array;
/** Null-padded `https://` base of every metadata URI. */
baseUri: ReadonlyUint8Array; maxBufferSize: number; assetIndex: number; contentsCount: number; backgroundCount: number; patternCount: number; maxDepth: number;
/** 0 configured (reserves escrowed), 1 ready (collection and tree bound). */
status: number; bump: number; feeVaultBump: number;  };

/** Gets the encoder for {@link ExclusiveSeriesStateArgs} account data. */
export function getExclusiveSeriesStateEncoder(): FixedSizeEncoder<ExclusiveSeriesStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['migrationVersion', getU8Encoder()], ['authority', getAddressEncoder()], ['template', getAddressEncoder()], ['bundle', getAddressEncoder()], ['collection', getAddressEncoder()], ['merkleTree', getAddressEncoder()], ['quantity', getU64Encoder()], ['minted', getU64Encoder()], ['rentReserve', getU64Encoder()], ['mintFeeLamports', getU64Encoder()], ['weights', fixPinaPodEncoderSize(getBytesEncoder(), 64)], ['bonusLamports', fixPinaPodEncoderSize(getBytesEncoder(), 128)], ['bonusCounts', fixPinaPodEncoderSize(getBytesEncoder(), 64)], ['bonusRemaining', fixPinaPodEncoderSize(getBytesEncoder(), 64)], ['tierMinted', fixPinaPodEncoderSize(getBytesEncoder(), 64)], ['namePrefix', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['symbol', fixPinaPodEncoderSize(getBytesEncoder(), 10)], ['baseUri', fixPinaPodEncoderSize(getBytesEncoder(), 96)], ['maxBufferSize', getU32Encoder()], ['assetIndex', getU8Encoder()], ['contentsCount', getU8Encoder()], ['backgroundCount', getU8Encoder()], ['patternCount', getU8Encoder()], ['maxDepth', getU8Encoder()], ['status', getU8Encoder()], ['bump', getU8Encoder()], ['feeVaultBump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 10, migrationVersion: 0 }));
}

/** Gets the decoder for {@link ExclusiveSeriesState} account data. */
export function getExclusiveSeriesStateDecoder(): FixedSizeDecoder<ExclusiveSeriesState> {
    return getStructDecoder([['discriminator', getPinaPodDiscriminatorDecoder(EXCLUSIVE_SERIES_STATE_DISCRIMINATOR, getU8Decoder())], ['migrationVersion', getPinaPodMigrationVersionDecoder(0, getU8Decoder())], ['authority', getAddressDecoder()], ['template', getAddressDecoder()], ['bundle', getAddressDecoder()], ['collection', getAddressDecoder()], ['merkleTree', getAddressDecoder()], ['quantity', getU64Decoder()], ['minted', getU64Decoder()], ['rentReserve', getU64Decoder()], ['mintFeeLamports', getU64Decoder()], ['weights', fixDecoderSize(getBytesDecoder(), 64)], ['bonusLamports', fixDecoderSize(getBytesDecoder(), 128)], ['bonusCounts', fixDecoderSize(getBytesDecoder(), 64)], ['bonusRemaining', fixDecoderSize(getBytesDecoder(), 64)], ['tierMinted', fixDecoderSize(getBytesDecoder(), 64)], ['namePrefix', fixDecoderSize(getBytesDecoder(), 32)], ['symbol', fixDecoderSize(getBytesDecoder(), 10)], ['baseUri', fixDecoderSize(getBytesDecoder(), 96)], ['maxBufferSize', getU32Decoder()], ['assetIndex', getU8Decoder()], ['contentsCount', getU8Decoder()], ['backgroundCount', getU8Decoder()], ['patternCount', getU8Decoder()], ['maxDepth', getU8Decoder()], ['status', getU8Decoder()], ['bump', getU8Decoder()], ['feeVaultBump', getU8Decoder()]]);
}

/** Gets the codec for {@link ExclusiveSeriesState} account data. */
export function getExclusiveSeriesStateCodec(): FixedSizeCodec<ExclusiveSeriesStateArgs, ExclusiveSeriesState> {
    return combineCodec(getExclusiveSeriesStateEncoder(), getExclusiveSeriesStateDecoder());
}

export function decodeExclusiveSeriesState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<ExclusiveSeriesState, TAddress>;
export function decodeExclusiveSeriesState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<ExclusiveSeriesState, TAddress>;
export function decodeExclusiveSeriesState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<ExclusiveSeriesState, TAddress> | MaybeAccount<ExclusiveSeriesState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getExclusiveSeriesStateDecoder());
}

export async function fetchExclusiveSeriesState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ExclusiveSeriesState, TAddress>> {
  const maybeAccount = await fetchMaybeExclusiveSeriesState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveSeriesState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ExclusiveSeriesState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeExclusiveSeriesState(maybeAccount);
}

export async function fetchAllExclusiveSeriesState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ExclusiveSeriesState>[]> {
  const maybeAccounts = await fetchAllMaybeExclusiveSeriesState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeExclusiveSeriesState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ExclusiveSeriesState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeExclusiveSeriesState(maybeAccount));
}

export async function fetchExclusiveSeriesStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveSeriesSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<ExclusiveSeriesState>> {
  const maybeAccount = await fetchMaybeExclusiveSeriesStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveSeriesStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveSeriesSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<ExclusiveSeriesState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findExclusiveSeriesPda(seeds, { programAddress });
  return await fetchMaybeExclusiveSeriesState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `ExclusiveSeriesState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (exclusiveSeriesStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ exclusiveSeriesState: address, payer }).make());
 * }
 * ```
 */
export function exclusiveSeriesStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 10) {
		return false;
	}
	return data[1]! < 0;
}
