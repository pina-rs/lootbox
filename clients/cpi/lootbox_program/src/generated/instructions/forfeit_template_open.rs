//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `forfeit_template_open` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ForfeitTemplateOpen<'account> {
	/// CPI account `caller`.
	/// Any signer may advance an expired FIFO head; the stored beneficiary and
	/// their exclusive claim rights are never changed.
	/// Required privileges: writable and signer.
	pub caller: &'account AccountView,

	/// CPI account `beneficiary`.
	/// Bound destination of the forfeit bounty: the creator-funded service
	/// budget compensates the beneficiary whose box burned, never the crank.
	/// Required privileges: writable.
	pub beneficiary: &'account AccountView,

	/// CPI account `template`.
	/// Required privileges: writable.
	pub template: &'account AccountView,

	/// CPI account `serviceVault`.
	/// Required privileges: writable.
	pub service_vault: &'account AccountView,

	/// CPI account `opening`.
	/// Required privileges: writable.
	pub opening: &'account AccountView,

	/// CPI account `randomness`.
	/// Required privileges: read-only.
	pub randomness: &'account AccountView,

	/// CPI account `systemProgram`.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `forfeit_template_open`.
	pub ix: ForfeitTemplateOpenIx,
}

/// Instruction arguments for the `forfeit_template_open` CPI call.
#[derive(Clone, Copy, Debug, Default)]
pub struct ForfeitTemplateOpenIx;

impl ForfeitTemplateOpenIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 2;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 2], ProgramError> {
		let mut data = [0u8; 2];
		data[..2].copy_from_slice(&FORFEIT_TEMPLATE_OPEN_DISCRIMINATOR);

		Ok(data)
	}
}

impl<'account> ForfeitTemplateOpen<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 7] = [
			CpiHandle::writable_signer(self.caller)?,
			CpiHandle::writable(self.beneficiary)?,
			CpiHandle::writable(self.template)?,
			CpiHandle::writable(self.service_vault)?,
			CpiHandle::writable(self.opening)?,
			CpiHandle::readonly(self.randomness),
			CpiHandle::readonly(self.system_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const FORFEIT_TEMPLATE_OPEN_DISCRIMINATOR: [u8; 2] = [36, 0];
