//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `prepare_prize_pool_item` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct PreparePrizePoolItem<'account, 'argument> {
	/// CPI account `authority`.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Required privileges: read-only.
	pub template: &'account AccountView,

	/// CPI account `bundle`.
	/// Required privileges: read-only.
	pub bundle: &'account AccountView,

	/// CPI account `prizePool`.
	/// Required privileges: writable.
	pub prize_pool: &'account AccountView,

	/// CPI account `prizePoolItem`.
	/// Required privileges: writable.
	pub prize_pool_item: &'account AccountView,

	/// CPI account `systemProgram`.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `prepare_prize_pool_item`.
	pub ix: PreparePrizePoolItemIx<'argument>,
}

/// Instruction arguments for the `prepare_prize_pool_item` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct PreparePrizePoolItemIx<'argument> {
	/// Instruction argument `itemBump`.
	pub item_bump: u8,

	/// Instruction argument `dataHash`.
	pub data_hash: [u8; 32],

	/// Instruction argument `creatorHash`.
	pub creator_hash: [u8; 32],

	/// Instruction argument `nonce`.
	pub nonce: u64,

	/// Instruction argument `index`.
	pub index: u32,

	/// Instruction argument `metadata`.
	pub metadata: &'argument [u8],
}

impl<'argument> PreparePrizePoolItemIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 593;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 593], ProgramError> {
		let mut data = [0u8; 593];
		data[..2].copy_from_slice(&PREPARE_PRIZE_POOL_ITEM_DISCRIMINATOR);
		data[2..3].copy_from_slice(&self.item_bump.to_le_bytes());
		data[3..35].copy_from_slice(&self.data_hash);
		data[35..67].copy_from_slice(&self.creator_hash);
		data[67..75].copy_from_slice(&self.nonce.to_le_bytes());
		data[75..79].copy_from_slice(&self.index.to_le_bytes());
		if self.metadata.len() > 512 {
			return Err(ProgramError::InvalidInstructionData);
		}
		data[79..79 + 2].copy_from_slice(&(self.metadata.len() as u16).to_le_bytes());
		for (index, value) in self.metadata.iter().enumerate() {
			let item_offset = 79 + 2 + index;
			data[item_offset..item_offset + 1].copy_from_slice(&value.to_le_bytes());
		}

		Ok(data)
	}
}

impl<'account, 'argument> PreparePrizePoolItem<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 6] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.template),
			CpiHandle::readonly(self.bundle),
			CpiHandle::writable(self.prize_pool)?,
			CpiHandle::writable(self.prize_pool_item)?,
			CpiHandle::readonly(self.system_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const PREPARE_PRIZE_POOL_ITEM_DISCRIMINATOR: [u8; 2] = [51, 0];
