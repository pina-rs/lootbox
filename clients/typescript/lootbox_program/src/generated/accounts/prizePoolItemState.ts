/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixPinaPodEncoderSize, getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findPrizePoolItemPda, type PrizePoolItemSeeds } from '../pdas';

export const PRIZE_POOL_ITEM_STATE_DISCRIMINATOR = 9;

export function getPrizePoolItemStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(PRIZE_POOL_ITEM_STATE_DISCRIMINATOR); }

export const PRIZE_POOL_ITEM_STATE_DISCRIMINATOR2 = 0;

export function getPrizePoolItemStateDiscriminator2Bytes(): ReadonlyUint8Array { return getU8Encoder().encode(PRIZE_POOL_ITEM_STATE_DISCRIMINATOR2); }

/**
 * Immutable identity and normalized metadata commitment for one Bubblegum leaf.
 *
 * Bubblegum authorities may change verification flags after custody, so claims
 * re-prove the current metadata while pinning every semantic field.
 */
export type PrizePoolItemState = { discriminator: number; migrationVersion: number; pool: Address; asset: Address; dataHash: ReadonlyUint8Array; creatorHash: ReadonlyUint8Array; semanticMetadataHash: ReadonlyUint8Array;
/** Restores the append-only accumulator when an unfinished tail is removed. */
previousManifestAccumulator: ReadonlyUint8Array; nonce: bigint; treeIndex: number; poolIndex: number;
/** 0 metadata-admitted, 1 transferred into `PrizePool` custody. */
status: number; bump: number;  };

export type PrizePoolItemStateArgs = { pool: Address; asset: Address; dataHash: ReadonlyUint8Array; creatorHash: ReadonlyUint8Array; semanticMetadataHash: ReadonlyUint8Array;
/** Restores the append-only accumulator when an unfinished tail is removed. */
previousManifestAccumulator: ReadonlyUint8Array; nonce: number | bigint; treeIndex: number; poolIndex: number;
/** 0 metadata-admitted, 1 transferred into `PrizePool` custody. */
status: number; bump: number;  };

/** Gets the encoder for {@link PrizePoolItemStateArgs} account data. */
export function getPrizePoolItemStateEncoder(): FixedSizeEncoder<PrizePoolItemStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['migrationVersion', getU8Encoder()], ['pool', getAddressEncoder()], ['asset', getAddressEncoder()], ['dataHash', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['creatorHash', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['semanticMetadataHash', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['previousManifestAccumulator', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['nonce', getU64Encoder()], ['treeIndex', getU32Encoder()], ['poolIndex', getU32Encoder()], ['status', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 9, migrationVersion: 0 }));
}

/** Gets the decoder for {@link PrizePoolItemState} account data. */
export function getPrizePoolItemStateDecoder(): FixedSizeDecoder<PrizePoolItemState> {
    return getStructDecoder([['discriminator', getPinaPodDiscriminatorDecoder(PRIZE_POOL_ITEM_STATE_DISCRIMINATOR, getU8Decoder())], ['migrationVersion', getPinaPodMigrationVersionDecoder(0, getU8Decoder())], ['pool', getAddressDecoder()], ['asset', getAddressDecoder()], ['dataHash', fixDecoderSize(getBytesDecoder(), 32)], ['creatorHash', fixDecoderSize(getBytesDecoder(), 32)], ['semanticMetadataHash', fixDecoderSize(getBytesDecoder(), 32)], ['previousManifestAccumulator', fixDecoderSize(getBytesDecoder(), 32)], ['nonce', getU64Decoder()], ['treeIndex', getU32Decoder()], ['poolIndex', getU32Decoder()], ['status', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link PrizePoolItemState} account data. */
export function getPrizePoolItemStateCodec(): FixedSizeCodec<PrizePoolItemStateArgs, PrizePoolItemState> {
    return combineCodec(getPrizePoolItemStateEncoder(), getPrizePoolItemStateDecoder());
}

export function decodePrizePoolItemState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<PrizePoolItemState, TAddress>;
export function decodePrizePoolItemState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<PrizePoolItemState, TAddress>;
export function decodePrizePoolItemState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<PrizePoolItemState, TAddress> | MaybeAccount<PrizePoolItemState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getPrizePoolItemStateDecoder());
}

export async function fetchPrizePoolItemState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PrizePoolItemState, TAddress>> {
  const maybeAccount = await fetchMaybePrizePoolItemState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePrizePoolItemState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PrizePoolItemState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePrizePoolItemState(maybeAccount);
}

export async function fetchAllPrizePoolItemState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PrizePoolItemState>[]> {
  const maybeAccounts = await fetchAllMaybePrizePoolItemState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePrizePoolItemState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PrizePoolItemState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePrizePoolItemState(maybeAccount));
}

export async function fetchPrizePoolItemStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PrizePoolItemSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<PrizePoolItemState>> {
  const maybeAccount = await fetchMaybePrizePoolItemStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePrizePoolItemStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PrizePoolItemSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<PrizePoolItemState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findPrizePoolItemPda(seeds, { programAddress });
  return await fetchMaybePrizePoolItemState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `PrizePoolItemState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (prizePoolItemStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ prizePoolItemState: address, payer }).make());
 * }
 * ```
 */
export function prizePoolItemStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 9) {
		return false;
	}
	return data[1]! < 0;
}
