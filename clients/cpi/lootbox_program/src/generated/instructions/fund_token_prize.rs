//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Escrows an SPL Token or Token-2022 prize in the next slot of a funding
/// bundle.
///
/// The template authority signs the transfer of the per-win amount times the
/// bundle quantity into the bundle's associated token account. The treasury
/// must be unlocked and not retired, and the escrow must end with exactly that
/// increase.
/// CPI call for the `fund_token_prize` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct FundTokenPrize<'account> {
	/// CPI account `authority`.
	/// Template authority; signs the token transfer from `source`.
	/// Required privileges: read-only and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA that must be unlocked and not retired; read only by the
	/// handler.
	/// Required privileges: writable.
	pub template: &'account AccountView,

	/// CPI account `bundle`.
	/// Funding bundle PDA of this template that records the prize.
	/// Required privileges: writable.
	pub bundle: &'account AccountView,

	/// CPI account `mint`.
	/// Prize mint owned by `token_program`; not wrapped SOL. Classic mints need
	/// no freeze authority; Token-2022 mints must pass the prize allowlist.
	/// Required privileges: read-only.
	pub mint: &'account AccountView,

	/// CPI account `source`.
	/// Token account debited for the deposit, including any transfer fee.
	/// Required privileges: writable.
	pub source: &'account AccountView,

	/// CPI account `escrow`.
	/// Bundle's associated token account for `mint`; must have no delegate or
	/// close authority and must not be frozen.
	/// Required privileges: writable.
	pub escrow: &'account AccountView,

	/// CPI account `tokenProgram`.
	/// SPL Token or Token-2022 program that owns `mint`, invoked for the
	/// transfer.
	/// Required privileges: read-only.
	pub token_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `fund_token_prize`.
	pub ix: FundTokenPrizeIx,
}

/// Instruction arguments for the `fund_token_prize` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct FundTokenPrizeIx {
	/// Instruction argument `amountPerWin`.
	pub amount_per_win: u64,

	/// Instruction argument `isNft`.
	pub is_nft: bool,
}

impl FundTokenPrizeIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 11;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 11], ProgramError> {
		let mut data = [0u8; 11];
		data[..2].copy_from_slice(&FUND_TOKEN_PRIZE_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.amount_per_win.to_le_bytes());
		data[10] = u8::from(self.is_nft);

		Ok(data)
	}
}

impl<'account> FundTokenPrize<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 7] = [
			CpiHandle::readonly_signer(self.authority),
			CpiHandle::writable(self.template)?,
			CpiHandle::writable(self.bundle)?,
			CpiHandle::readonly(self.mint),
			CpiHandle::writable(self.source)?,
			CpiHandle::writable(self.escrow)?,
			CpiHandle::readonly(self.token_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const FUND_TOKEN_PRIZE_DISCRIMINATOR: [u8; 2] = [13, 0];
