//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `mint_template_boxes` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct MintTemplateBoxes<'account> {
	/// CPI account `authority`.
	/// Required privileges: read-only.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Required privileges: writable.
	pub template: &'account AccountView,

	/// CPI account `boxMint`.
	/// Required privileges: writable.
	pub box_mint: &'account AccountView,

	/// CPI account `recipientBoxAccount`.
	/// Required privileges: writable.
	pub recipient_box_account: &'account AccountView,

	/// CPI account `boxTokenProgram`.
	/// Required privileges: read-only.
	pub box_token_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `mint_template_boxes`.
	pub ix: MintTemplateBoxesIx,
}

/// Instruction arguments for the `mint_template_boxes` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct MintTemplateBoxesIx {
	/// Instruction argument `amount`.
	pub amount: u64,
}

impl MintTemplateBoxesIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 10;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 10], ProgramError> {
		let mut data = [0u8; 10];
		data[..2].copy_from_slice(&MINT_TEMPLATE_BOXES_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.amount.to_le_bytes());

		Ok(data)
	}
}

impl<'account> MintTemplateBoxes<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::readonly(self.authority),
			CpiHandle::writable(self.template)?,
			CpiHandle::writable(self.box_mint)?,
			CpiHandle::writable(self.recipient_box_account)?,
			CpiHandle::readonly(self.box_token_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const MINT_TEMPLATE_BOXES_DISCRIMINATOR: [u8; 2] = [15, 0];
