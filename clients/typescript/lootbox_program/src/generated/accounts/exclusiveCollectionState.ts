/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixPinaPodEncoderSize, getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findExclusiveCollectionPda, type ExclusiveCollectionSeeds } from '../pdas';

export const EXCLUSIVE_COLLECTION_STATE_DISCRIMINATOR = 10;

export function getExclusiveCollectionStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_COLLECTION_STATE_DISCRIMINATOR); }

export const EXCLUSIVE_COLLECTION_STATE_DISCRIMINATOR2 = 0;

export function getExclusiveCollectionStateDiscriminator2Bytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_COLLECTION_STATE_DISCRIMINATOR2); }

/**
 * A protocol-level Exclusive Lootbox NFT collection shared by many lootboxes.
 *
 * Drafts accept layer tables from the admin. Publishing freezes them under
 * `layers_hash`; afterwards the admin can only append trees. Every mint
 * takes the next global serial and lands in `active_tree`.
 */
export type ExclusiveCollectionState = { discriminator: number; migrationVersion: number;
/**
 * Loads layers, appends trees, and publishes; a multisig on mainnet.
 *
 * Part of the PDA seeds, so it can never change.
 */
admin: Address;
/** Metaplex Core collection whose update authority is this PDA. */
coreCollection: Address;
/**
 * Bubblegum V2 tree that receives the next mint; zero until appended.
 *
 * Each `appendExclusiveTree` replaces it, and claims must mint into it.
 */
activeTree: Address;
/** Commitment to every frozen term; zero until published. */
layersHash: ReadonlyUint8Array;
/**
 * Admin-chosen identifier that separates this admin's collections in the
 * PDA seeds; set at creation.
 */
collectionId: bigint;
/** Editions minted so far; the next global serial is `minted + 1`. */
minted: bigint;
/**
 * Unix time from which bundles may attach.
 *
 * Seconds, inclusive; set at creation and earlier than `attach_closes_at`.
 */
attachOpensAt: bigint;
/**
 * Unix time from which bundles may no longer attach.
 *
 * Seconds, exclusive; checked only when a bundle attaches.
 */
attachClosesAt: bigint;
/** Trees appended so far; incremented by each `appendExclusiveTree`. */
treeCount: number;
/**
 * Trait count of each layer, bottom to top.
 *
 * Each configured layer has 1 through 64 traits; entries at or above
 * `layer_count` must be zero at publication.
 */
traitCounts: ReadonlyUint8Array;
/** Twelve layers of sixty-four little-endian `u32` trait weights. */
weights: ReadonlyUint8Array;
/**
 * Null-padded UTF-8 name prefix; minted names are `{prefix} #{serial}`.
 *
 * At most 20 bytes, so any ten-digit serial fits the 32-byte name cap.
 */
namePrefix: ReadonlyUint8Array;
/** Null-padded UTF-8 symbol. */
symbol: ReadonlyUint8Array;
/** Null-padded `https://` base of every metadata URI. */
baseUri: ReadonlyUint8Array;
/** Number of trait layers, from 1 through 12; fixed at creation. */
layerCount: number;
/**
 * 0 draft, 1 published.
 *
 * `publishExclusiveCollection` moves it from draft to published once.
 */
status: number;
/**
 * Canonical bump of this PDA
 * `["exclusive-collection", admin, collection_id]`.
 */
bump: number;  };

export type ExclusiveCollectionStateArgs = {
/**
 * Loads layers, appends trees, and publishes; a multisig on mainnet.
 *
 * Part of the PDA seeds, so it can never change.
 */
admin: Address;
/** Metaplex Core collection whose update authority is this PDA. */
coreCollection: Address;
/**
 * Bubblegum V2 tree that receives the next mint; zero until appended.
 *
 * Each `appendExclusiveTree` replaces it, and claims must mint into it.
 */
activeTree: Address;
/** Commitment to every frozen term; zero until published. */
layersHash: ReadonlyUint8Array;
/**
 * Admin-chosen identifier that separates this admin's collections in the
 * PDA seeds; set at creation.
 */
collectionId: number | bigint;
/** Editions minted so far; the next global serial is `minted + 1`. */
minted: number | bigint;
/**
 * Unix time from which bundles may attach.
 *
 * Seconds, inclusive; set at creation and earlier than `attach_closes_at`.
 */
attachOpensAt: number | bigint;
/**
 * Unix time from which bundles may no longer attach.
 *
 * Seconds, exclusive; checked only when a bundle attaches.
 */
attachClosesAt: number | bigint;
/** Trees appended so far; incremented by each `appendExclusiveTree`. */
treeCount: number;
/**
 * Trait count of each layer, bottom to top.
 *
 * Each configured layer has 1 through 64 traits; entries at or above
 * `layer_count` must be zero at publication.
 */
traitCounts: ReadonlyUint8Array;
/** Twelve layers of sixty-four little-endian `u32` trait weights. */
weights: ReadonlyUint8Array;
/**
 * Null-padded UTF-8 name prefix; minted names are `{prefix} #{serial}`.
 *
 * At most 20 bytes, so any ten-digit serial fits the 32-byte name cap.
 */
namePrefix: ReadonlyUint8Array;
/** Null-padded UTF-8 symbol. */
symbol: ReadonlyUint8Array;
/** Null-padded `https://` base of every metadata URI. */
baseUri: ReadonlyUint8Array;
/** Number of trait layers, from 1 through 12; fixed at creation. */
layerCount: number;
/**
 * 0 draft, 1 published.
 *
 * `publishExclusiveCollection` moves it from draft to published once.
 */
status: number;
/**
 * Canonical bump of this PDA
 * `["exclusive-collection", admin, collection_id]`.
 */
bump: number;  };

/** Gets the encoder for {@link ExclusiveCollectionStateArgs} account data. */
export function getExclusiveCollectionStateEncoder(): FixedSizeEncoder<ExclusiveCollectionStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['migrationVersion', getU8Encoder()], ['admin', getAddressEncoder()], ['coreCollection', getAddressEncoder()], ['activeTree', getAddressEncoder()], ['layersHash', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['collectionId', getU64Encoder()], ['minted', getU64Encoder()], ['attachOpensAt', getI64Encoder()], ['attachClosesAt', getI64Encoder()], ['treeCount', getU32Encoder()], ['traitCounts', fixPinaPodEncoderSize(getBytesEncoder(), 12)], ['weights', fixPinaPodEncoderSize(getBytesEncoder(), 3072)], ['namePrefix', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['symbol', fixPinaPodEncoderSize(getBytesEncoder(), 10)], ['baseUri', fixPinaPodEncoderSize(getBytesEncoder(), 128)], ['layerCount', getU8Encoder()], ['status', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 10, migrationVersion: 0 }));
}

/** Gets the decoder for {@link ExclusiveCollectionState} account data. */
export function getExclusiveCollectionStateDecoder(): FixedSizeDecoder<ExclusiveCollectionState> {
    return getStructDecoder([['discriminator', getPinaPodDiscriminatorDecoder(EXCLUSIVE_COLLECTION_STATE_DISCRIMINATOR, getU8Decoder())], ['migrationVersion', getPinaPodMigrationVersionDecoder(0, getU8Decoder())], ['admin', getAddressDecoder()], ['coreCollection', getAddressDecoder()], ['activeTree', getAddressDecoder()], ['layersHash', fixDecoderSize(getBytesDecoder(), 32)], ['collectionId', getU64Decoder()], ['minted', getU64Decoder()], ['attachOpensAt', getI64Decoder()], ['attachClosesAt', getI64Decoder()], ['treeCount', getU32Decoder()], ['traitCounts', fixDecoderSize(getBytesDecoder(), 12)], ['weights', fixDecoderSize(getBytesDecoder(), 3072)], ['namePrefix', fixDecoderSize(getBytesDecoder(), 32)], ['symbol', fixDecoderSize(getBytesDecoder(), 10)], ['baseUri', fixDecoderSize(getBytesDecoder(), 128)], ['layerCount', getU8Decoder()], ['status', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link ExclusiveCollectionState} account data. */
export function getExclusiveCollectionStateCodec(): FixedSizeCodec<ExclusiveCollectionStateArgs, ExclusiveCollectionState> {
    return combineCodec(getExclusiveCollectionStateEncoder(), getExclusiveCollectionStateDecoder());
}

export function decodeExclusiveCollectionState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<ExclusiveCollectionState, TAddress>;
export function decodeExclusiveCollectionState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<ExclusiveCollectionState, TAddress>;
export function decodeExclusiveCollectionState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<ExclusiveCollectionState, TAddress> | MaybeAccount<ExclusiveCollectionState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getExclusiveCollectionStateDecoder());
}

export async function fetchExclusiveCollectionState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ExclusiveCollectionState, TAddress>> {
  const maybeAccount = await fetchMaybeExclusiveCollectionState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveCollectionState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ExclusiveCollectionState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeExclusiveCollectionState(maybeAccount);
}

export async function fetchAllExclusiveCollectionState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ExclusiveCollectionState>[]> {
  const maybeAccounts = await fetchAllMaybeExclusiveCollectionState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeExclusiveCollectionState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ExclusiveCollectionState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeExclusiveCollectionState(maybeAccount));
}

export async function fetchExclusiveCollectionStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveCollectionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<ExclusiveCollectionState>> {
  const maybeAccount = await fetchMaybeExclusiveCollectionStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveCollectionStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveCollectionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<ExclusiveCollectionState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findExclusiveCollectionPda(seeds, { programAddress });
  return await fetchMaybeExclusiveCollectionState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const EXCLUSIVE_COLLECTION_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `ExclusiveCollectionState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (exclusiveCollectionStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ exclusiveCollectionState: address, payer }).make());
 * }
 * ```
 */
export function exclusiveCollectionStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 10) {
		return false;
	}
	return data[1]! < 0;
}
