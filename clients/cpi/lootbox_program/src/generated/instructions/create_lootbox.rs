//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::Address;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Creates a lootbox definition PDA and its SOL vault PDA, both paid for by the
/// signing authority. The box mint must be an existing classic SPL mint with
/// zero decimals, zero supply, the lootbox PDA as mint authority, and no freeze
/// authority. The new lootbox is unsealed and has no outcomes.
/// CPI call for the `create_lootbox` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct CreateLootbox<'account, 'argument> {
	/// CPI account `authority`.
	/// Creator that becomes the lootbox authority and pays rent for the lootbox
	/// and vault accounts. Signer; seed of the lootbox PDA.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `boxMint`.
	/// Existing classic SPL mint for the boxes. Must have zero decimals, zero
	/// supply, the lootbox PDA as mint authority, and no freeze authority.
	/// Required privileges: read-only.
	pub box_mint: &'account AccountView,

	/// CPI account `lootbox`.
	/// Lootbox PDA `["lootbox", authority, id]`, created here.
	/// Required privileges: writable.
	pub lootbox: &'account AccountView,

	/// CPI account `vault`.
	/// Vault PDA `["vault", lootbox]`, created here; its post-creation balance
	/// becomes the rent reserve.
	/// Required privileges: writable.
	pub vault: &'account AccountView,

	/// CPI account `systemProgram`.
	/// System program, invoked to create the lootbox and vault accounts.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// CPI account `tokenProgram`.
	/// SPL Token program that owns the box mint.
	/// Required privileges: read-only.
	pub token_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `create_lootbox`.
	pub ix: CreateLootboxIx<'argument>,
}

/// Instruction arguments for the `create_lootbox` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct CreateLootboxIx<'argument> {
	/// Instruction argument `id`.
	pub id: u64,

	/// Instruction argument `maxSupply`.
	pub max_supply: u64,

	/// Instruction argument `oracleProgram`.
	pub oracle_program: &'argument Address,

	/// Instruction argument `oracleQueue`.
	pub oracle_queue: &'argument Address,

	/// Instruction argument `bump`.
	pub bump: u8,

	/// Instruction argument `vaultBump`.
	pub vault_bump: u8,
}

impl<'argument> CreateLootboxIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 84;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 84], ProgramError> {
		let mut data = [0u8; 84];
		data[..2].copy_from_slice(&CREATE_LOOTBOX_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.id.to_le_bytes());
		data[10..18].copy_from_slice(&self.max_supply.to_le_bytes());
		data[18..50].copy_from_slice(self.oracle_program.as_ref());
		data[50..82].copy_from_slice(self.oracle_queue.as_ref());
		data[82..83].copy_from_slice(&self.bump.to_le_bytes());
		data[83..84].copy_from_slice(&self.vault_bump.to_le_bytes());

		Ok(data)
	}
}

impl<'account, 'argument> CreateLootbox<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 6] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.box_mint),
			CpiHandle::writable(self.lootbox)?,
			CpiHandle::writable(self.vault)?,
			CpiHandle::readonly(self.system_program),
			CpiHandle::readonly(self.token_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CREATE_LOOTBOX_DISCRIMINATOR: [u8; 2] = [0, 0];
