/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { getZeroPodDiscriminatorDecoder } from "../zeropodCodecs";
import { combineCodec, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { LOOTBOX_PROGRAM_PROGRAM_ADDRESS } from '../programs';

export const SEAL_DISCRIMINATOR = 3;

export function getSealDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(SEAL_DISCRIMINATOR); }

export type SealInstruction<TProgram extends string = typeof LOOTBOX_PROGRAM_PROGRAM_ADDRESS, TAccountAuthority extends string | AccountMeta<string> = string, TAccountLootbox extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountLootbox extends string ? WritableAccount<TAccountLootbox> : TAccountLootbox, ...TRemainingAccounts]>;

export type SealInstructionData = { discriminator: number;  };

export type SealInstructionDataArgs = {  };

export function getSealInstructionDataEncoder(): FixedSizeEncoder<SealInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()]]), (value) => ({ ...value, discriminator: 3 }));
}

export function getSealInstructionDataDecoder(): FixedSizeDecoder<SealInstructionData> {
    return getStructDecoder([['discriminator', getZeroPodDiscriminatorDecoder(SEAL_DISCRIMINATOR, getU8Decoder())]]);
}

export function getSealInstructionDataCodec(): FixedSizeCodec<SealInstructionDataArgs, SealInstructionData> {
    return combineCodec(getSealInstructionDataEncoder(), getSealInstructionDataDecoder());
}

export type SealInput<TAccountAuthority extends string = string, TAccountLootbox extends string = string> =  {
  authority: TransactionSigner<TAccountAuthority>;
lootbox: Address<TAccountLootbox>;
}

export function getSealInstruction<TAccountAuthority extends string, TAccountLootbox extends string, TProgramAddress extends Address = typeof LOOTBOX_PROGRAM_PROGRAM_ADDRESS>(input: SealInput<TAccountAuthority, TAccountLootbox>, config?: { programAddress?: TProgramAddress } ): SealInstruction<TProgramAddress, TAccountAuthority, TAccountLootbox> {
  // Program address.
const programAddress = config?.programAddress ?? LOOTBOX_PROGRAM_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { authority: { value: input.authority ?? null, isWritable: false }, lootbox: { value: input.lootbox ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("authority", accounts.authority), getAccountMeta("lootbox", accounts.lootbox)], data: getSealInstructionDataEncoder().encode({}), programAddress } as SealInstruction<TProgramAddress, TAccountAuthority, TAccountLootbox>);
}

export type ParsedSealInstruction<TProgram extends string = typeof LOOTBOX_PROGRAM_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
authority: TAccountMetas[0];
lootbox: TAccountMetas[1];
};
data: SealInstructionData; };

export function parseSealInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSealInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { authority: getNextAccount(), lootbox: getNextAccount() }, data: getSealInstructionDataDecoder().decode(instruction.data) };
}