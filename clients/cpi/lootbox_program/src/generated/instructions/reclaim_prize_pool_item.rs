//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Return an unassigned deposited leaf to the template authority.
///
/// The template authority signs. An unsealed pool, with an unlocked treasury,
/// a funding bundle, and no prepared item, releases only its last deposit and
/// restores the prior accumulator. A sealed pool releases an unassigned item
/// while its bundle is funding with an unlocked treasury, or after the
/// template retires with zero box supply and no pending openings. The pool PDA
/// signs the Bubblegum transfer and the item PDA closes to the authority.
/// CPI call for the `reclaim_prize_pool_item` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ReclaimPrizePoolItem<'account, 'argument> {
	/// CPI account `authority`.
	/// Template authority; signs, receives the leaf and the item rent, and
	/// receives rent freed by shrinking an unsealed pool.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA that owns `bundle`.
	/// Required privileges: read-only.
	pub template: &'account AccountView,

	/// CPI account `boxMint`.
	/// Template's Token-2022 box mint; its supply must be zero for an active
	/// bundle. Read only when the pool is sealed.
	/// Required privileges: read-only.
	pub box_mint: &'account AccountView,

	/// CPI account `bundle`.
	/// Bundle PDA; a sealed reclaim advances the slot's released count and
	/// may set its reclaimed bit.
	/// Required privileges: writable.
	pub bundle: &'account AccountView,

	/// CPI account `prizePool`.
	/// `PrizePoolState` PDA that signs the transfer; its cursor rewinds or its
	/// reclaimed count advances.
	/// Required privileges: writable.
	pub prize_pool: &'account AccountView,

	/// CPI account `prizePoolItem`.
	/// Deposited item PDA at `pool_index`, closed here.
	/// Required privileges: writable.
	pub prize_pool_item: &'account AccountView,

	/// CPI account `treeConfig`.
	/// Bubblegum tree config of `merkle_tree`, validated by Bubblegum.
	/// Required privileges: read-only.
	pub tree_config: &'account AccountView,

	/// CPI account `merkleTree`.
	/// Pool's pinned tree that holds the leaf; Bubblegum rewrites it.
	/// Required privileges: writable.
	pub merkle_tree: &'account AccountView,

	/// CPI account `bubblegumProgram`.
	/// Bubblegum program, invoked to transfer the compressed NFT.
	/// Required privileges: read-only.
	pub bubblegum_program: &'account AccountView,

	/// CPI account `logWrapper`.
	/// SPL Noop program used by Bubblegum as its log wrapper.
	/// Required privileges: read-only.
	pub log_wrapper: &'account AccountView,

	/// CPI account `compressionProgram`.
	/// SPL Account Compression program that owns `merkle_tree`.
	/// Required privileges: read-only.
	pub compression_program: &'account AccountView,

	/// CPI account `systemProgram`.
	/// System program, passed to Bubblegum.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// CPI account `proofAccounts`.
	/// Merkle proof nodes in leaf-to-root order.
	///
	/// Passed as zero through 16 readonly remaining accounts; the tree's canopy
	/// supplies the rest of the path.
	/// Required privileges: read-only.
	pub proof_accounts: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `reclaim_prize_pool_item`.
	pub ix: ReclaimPrizePoolItemIx<'argument>,
}

/// Instruction arguments for the `reclaim_prize_pool_item` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct ReclaimPrizePoolItemIx<'argument> {
	/// Instruction argument `poolIndex`.
	pub pool_index: u32,

	/// Instruction argument `root`.
	pub root: [u8; 32],

	/// Instruction argument `dataHash`.
	pub data_hash: [u8; 32],

	/// Instruction argument `creatorHash`.
	pub creator_hash: [u8; 32],

	/// Instruction argument `nonce`.
	pub nonce: u64,

	/// Instruction argument `index`.
	pub index: u32,

	/// Instruction argument `metadata`.
	pub metadata: &'argument [u8],
}

impl<'argument> ReclaimPrizePoolItemIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 628;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 628], ProgramError> {
		let mut data = [0u8; 628];
		data[..2].copy_from_slice(&RECLAIM_PRIZE_POOL_ITEM_DISCRIMINATOR);
		data[2..6].copy_from_slice(&self.pool_index.to_le_bytes());
		data[6..38].copy_from_slice(&self.root);
		data[38..70].copy_from_slice(&self.data_hash);
		data[70..102].copy_from_slice(&self.creator_hash);
		data[102..110].copy_from_slice(&self.nonce.to_le_bytes());
		data[110..114].copy_from_slice(&self.index.to_le_bytes());
		if self.metadata.len() > 512 {
			return Err(ProgramError::InvalidInstructionData);
		}
		data[114..114 + 2].copy_from_slice(&(self.metadata.len() as u16).to_le_bytes());
		for (index, value) in self.metadata.iter().enumerate() {
			let item_offset = 114 + 2 + index;
			data[item_offset..item_offset + 1].copy_from_slice(&value.to_le_bytes());
		}

		Ok(data)
	}
}

impl<'account, 'argument> ReclaimPrizePoolItem<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 13] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.template),
			CpiHandle::readonly(self.box_mint),
			CpiHandle::writable(self.bundle)?,
			CpiHandle::writable(self.prize_pool)?,
			CpiHandle::writable(self.prize_pool_item)?,
			CpiHandle::readonly(self.tree_config),
			CpiHandle::writable(self.merkle_tree)?,
			CpiHandle::readonly(self.bubblegum_program),
			CpiHandle::readonly(self.log_wrapper),
			CpiHandle::readonly(self.compression_program),
			CpiHandle::readonly(self.system_program),
			CpiHandle::readonly(self.proof_accounts),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const RECLAIM_PRIZE_POOL_ITEM_DISCRIMINATOR: [u8; 2] = [49, 0];
