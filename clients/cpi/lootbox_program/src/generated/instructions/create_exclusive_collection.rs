//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Create a draft Exclusive Lootbox NFT collection and its Core collection.
///
/// The admin signs and pays. Validates the text fields, a layer count from 1
/// through 12, and an attach window that opens before it closes. Creates the
/// `ExclusiveCollectionState` PDA and a Core collection, named after the
/// prefix with the URI `{base_uri}collection.json`, whose update authority is
/// that PDA.
/// CPI call for the `create_exclusive_collection` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct CreateExclusiveCollection<'account> {
	/// CPI account `admin`.
	/// Collection admin; signs, pays both accounts' rent, and is recorded as
	/// `admin`.
	/// Required privileges: writable and signer.
	pub admin: &'account AccountView,

	/// CPI account `exclusiveCollection`.
	/// Empty collection PDA `["exclusive-collection", admin, collection_id]`,
	/// created here.
	/// Required privileges: writable.
	pub exclusive_collection: &'account AccountView,

	/// CPI account `coreCollection`.
	/// Fresh Core collection keypair; its update authority becomes the PDA.
	///
	/// Signs, and is created here by Core.
	/// Required privileges: writable and signer.
	pub core_collection: &'account AccountView,

	/// CPI account `coreProgram`.
	/// Metaplex Core program, invoked to create the collection.
	/// Required privileges: read-only.
	pub core_program: &'account AccountView,

	/// CPI account `systemProgram`.
	/// System program, invoked to create both accounts.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `create_exclusive_collection`.
	pub ix: CreateExclusiveCollectionIx,
}

/// Instruction arguments for the `create_exclusive_collection` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct CreateExclusiveCollectionIx {
	/// Instruction argument `collectionId`.
	pub collection_id: u64,

	/// Instruction argument `attachOpensAt`.
	pub attach_opens_at: i64,

	/// Instruction argument `attachClosesAt`.
	pub attach_closes_at: i64,

	/// Instruction argument `layerCount`.
	pub layer_count: u8,

	/// Instruction argument `bump`.
	pub bump: u8,

	/// Instruction argument `namePrefix`.
	pub name_prefix: [u8; 32],

	/// Instruction argument `symbol`.
	pub symbol: [u8; 10],

	/// Instruction argument `baseUri`.
	pub base_uri: [u8; 128],
}

impl CreateExclusiveCollectionIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 198;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 198], ProgramError> {
		let mut data = [0u8; 198];
		data[..2].copy_from_slice(&CREATE_EXCLUSIVE_COLLECTION_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.collection_id.to_le_bytes());
		data[10..18].copy_from_slice(&self.attach_opens_at.to_le_bytes());
		data[18..26].copy_from_slice(&self.attach_closes_at.to_le_bytes());
		data[26..27].copy_from_slice(&self.layer_count.to_le_bytes());
		data[27..28].copy_from_slice(&self.bump.to_le_bytes());
		data[28..60].copy_from_slice(&self.name_prefix);
		data[60..70].copy_from_slice(&self.symbol);
		data[70..198].copy_from_slice(&self.base_uri);

		Ok(data)
	}
}

impl<'account> CreateExclusiveCollection<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::writable_signer(self.admin)?,
			CpiHandle::writable(self.exclusive_collection)?,
			CpiHandle::writable_signer(self.core_collection)?,
			CpiHandle::readonly(self.core_program),
			CpiHandle::readonly(self.system_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CREATE_EXCLUSIVE_COLLECTION_DISCRIMINATOR: [u8; 2] = [53, 0];
