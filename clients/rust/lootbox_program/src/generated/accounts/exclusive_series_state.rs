//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct ExclusiveSeriesState {
/// Immutable rarity rules, art references, bonus reserves, and mint counters of
/// one template's Exclusive Lootbox NFT series.
///
/// The PDA is the Bubblegum tree creator and the Core collection update
/// authority. Its lamports hold rent plus every unpaid tier bonus:
/// `lamports >= rent_reserve + sum(bonus_remaining[k] * bonus_lamports[k])`.
/// A separate zero-data fee vault PDA prepays Bubblegum's per-mint fee, so a
/// claim costs its submitter only the transaction fee.
	pub discriminator: u8,
	pub migration_version: u8,
	pub authority: solana_pubkey::Pubkey,
	pub template: solana_pubkey::Pubkey,
	pub bundle: solana_pubkey::Pubkey,
	/// Metaplex Core collection; zero until the series is initialized.
	pub collection: solana_pubkey::Pubkey,
	/// Bubblegum V2 tree; zero until the series is initialized.
	pub merkle_tree: solana_pubkey::Pubkey,
	/// Consolation copies the bound bundle slot can ever mint.
	pub quantity: u64,
	/// Editions minted so far; the next serial is `minted + 1`.
	pub minted: u64,
	/// Rent-exempt balance kept apart from the bonus reserves.
	pub rent_reserve: u64,
	/// Bubblegum mint fee escrowed per copy in the fee vault at creation.
	pub mint_fee_lamports: u64,
	/// Sixteen little-endian `u32` tier weights.
	pub weights: [u8; 64],
	/// Sixteen little-endian `u64` lamport bonuses paid per tier win.
	pub bonus_lamports: [u8; 128],
	/// Sixteen little-endian `u32` bonus counts funded at creation.
	pub bonus_counts: [u8; 64],
	/// Sixteen little-endian `u32` bonuses still escrowed.
	pub bonus_remaining: [u8; 64],
	/// Sixteen little-endian `u32` editions minted per tier.
	pub tier_minted: [u8; 64],
	/// Null-padded UTF-8 name prefix; minted names are `{prefix} #{serial}`.
	pub name_prefix: [u8; 32],
	/// Null-padded UTF-8 symbol.
	pub symbol: [u8; 10],
	/// Null-padded `https://` base of every metadata URI.
	pub base_uri: [u8; 96],
	pub max_buffer_size: u32,
	pub asset_index: u8,
	pub contents_count: u8,
	pub background_count: u8,
	pub pattern_count: u8,
	pub max_depth: u8,
	/// 0 configured (reserves escrowed), 1 ready (collection and tree bound).
	pub status: u8,
	pub bump: u8,
	pub fee_vault_bump: u8,
}

pub const EXCLUSIVE_SERIES_STATE_DISCRIMINATOR: u8 = 10u8;

pub const EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION: u8 = 0u8;

impl ExclusiveSeriesState {
	pub const LEN: usize = core::mem::size_of::<ExclusiveSeriesStateZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut ExclusiveSeriesStateZc),
	) -> Result<&mut ExclusiveSeriesStateZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = EXCLUSIVE_SERIES_STATE_DISCRIMINATOR;
			account.migration_version = EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(data: &[u8]) -> Result<&ExclusiveSeriesStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != EXCLUSIVE_SERIES_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(data: &mut [u8]) -> Result<&mut ExclusiveSeriesStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != EXCLUSIVE_SERIES_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl ExclusiveSeriesState {
	pub fn find_pda(template: &solana_pubkey::Pubkey) -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&[
				"exclusive-series".as_bytes(),
				template.as_ref(),
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}

	pub fn create_pda(template: &solana_pubkey::Pubkey, bump: u8) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&[
				"exclusive-series".as_bytes(),
				template.as_ref(),
				&[bump],
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}
}


/// Whether raw account bytes are stale for this contract: the envelope names this account's discriminator and carries a version older than
/// [`EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION`]. Current or foreign bytes return false; decoding explains the difference.
///
/// Version 0 is the initial version, so no bytes can ever be stale.
pub fn exclusive_series_state_needs_migration(_data: &[u8]) -> bool {
	false
}


/// Why `ExclusiveSeriesState::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ExclusiveSeriesStateVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version predates this client: migrate the account on-chain, then retry.
	Stale { stored: u8 },
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for ExclusiveSeriesStateVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid ExclusiveSeriesState account data"),
			Self::Stale { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data predates this client; migrate it by sending a transaction to the program, or decode it with a client generated from an older IDL)"
			),
			Self::Future { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data was written by a newer program; upgrade this client)"
			),
		}
	}
}

impl ExclusiveSeriesState {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`ExclusiveSeriesState::from_bytes`].
	pub fn try_from_bytes(
		data: &[u8],
	) -> Result<&ExclusiveSeriesStateZc, ExclusiveSeriesStateVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| ExclusiveSeriesStateVersionError::InvalidData)?;
		if account.discriminator != EXCLUSIVE_SERIES_STATE_DISCRIMINATOR {
			return Err(ExclusiveSeriesStateVersionError::InvalidData);
		}
		if account.migration_version > EXCLUSIVE_SERIES_STATE_MIGRATION_VERSION {
			return Err(ExclusiveSeriesStateVersionError::Future { stored: account.migration_version });
		}
		Ok(account)
	}
}

#[cfg(test)]
mod exclusive_series_state_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<ExclusiveSeriesStateZc>()];
		data[..1].copy_from_slice(&[10]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let error = ExclusiveSeriesState::try_from_bytes(&envelope(1_u8)).err().expect("a future envelope must fail");
		assert_eq!(error, ExclusiveSeriesStateVersionError::Future { stored: 1 });
		assert_eq!(ExclusiveSeriesStateVersionError::Future { stored: 1 }.to_string(), "migration version mismatch: expected 0, received 1 (the data was written by a newer program; upgrade this client)");
		assert!(
			ExclusiveSeriesState::try_from_bytes(&envelope(0_u8)).is_ok(),
			"the current version must decode",
		);
	}
}
