/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixPinaPodEncoderSize, getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findExclusiveAttachmentPda, type ExclusiveAttachmentSeeds } from '../pdas';

export const EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR = 11;

export function getExclusiveAttachmentStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR); }

export const EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR2 = 0;

export function getExclusiveAttachmentStateDiscriminator2Bytes(): ReadonlyUint8Array { return getU8Encoder().encode(EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR2); }

/**
 * One bundle slot's promise of Exclusive Lootbox NFTs from a collection.
 *
 * The PDA commits the slot to the collection's frozen layers and owns a
 * zero-data fee vault prepaying Bubblegum's per-mint fee, so a claim costs
 * its submitter only the transaction fee.
 */
export type ExclusiveAttachmentState = { discriminator: number; migrationVersion: number; template: Address; bundle: Address; collection: Address;
/** The collection's frozen `layers_hash` at attach time. */
layersHash: ReadonlyUint8Array;
/** Copies the bound bundle slot can ever mint. */
quantity: bigint;
/** Copies minted through this attachment. */
minted: bigint;
/** Bubblegum mint fee escrowed per copy. */
mintFeeLamports: bigint; assetIndex: number; bump: number; feeVaultBump: number;  };

export type ExclusiveAttachmentStateArgs = { template: Address; bundle: Address; collection: Address;
/** The collection's frozen `layers_hash` at attach time. */
layersHash: ReadonlyUint8Array;
/** Copies the bound bundle slot can ever mint. */
quantity: number | bigint;
/** Copies minted through this attachment. */
minted: number | bigint;
/** Bubblegum mint fee escrowed per copy. */
mintFeeLamports: number | bigint; assetIndex: number; bump: number; feeVaultBump: number;  };

/** Gets the encoder for {@link ExclusiveAttachmentStateArgs} account data. */
export function getExclusiveAttachmentStateEncoder(): FixedSizeEncoder<ExclusiveAttachmentStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['migrationVersion', getU8Encoder()], ['template', getAddressEncoder()], ['bundle', getAddressEncoder()], ['collection', getAddressEncoder()], ['layersHash', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['quantity', getU64Encoder()], ['minted', getU64Encoder()], ['mintFeeLamports', getU64Encoder()], ['assetIndex', getU8Encoder()], ['bump', getU8Encoder()], ['feeVaultBump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 11, migrationVersion: 0 }));
}

/** Gets the decoder for {@link ExclusiveAttachmentState} account data. */
export function getExclusiveAttachmentStateDecoder(): FixedSizeDecoder<ExclusiveAttachmentState> {
    return getStructDecoder([['discriminator', getPinaPodDiscriminatorDecoder(EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR, getU8Decoder())], ['migrationVersion', getPinaPodMigrationVersionDecoder(0, getU8Decoder())], ['template', getAddressDecoder()], ['bundle', getAddressDecoder()], ['collection', getAddressDecoder()], ['layersHash', fixDecoderSize(getBytesDecoder(), 32)], ['quantity', getU64Decoder()], ['minted', getU64Decoder()], ['mintFeeLamports', getU64Decoder()], ['assetIndex', getU8Decoder()], ['bump', getU8Decoder()], ['feeVaultBump', getU8Decoder()]]);
}

/** Gets the codec for {@link ExclusiveAttachmentState} account data. */
export function getExclusiveAttachmentStateCodec(): FixedSizeCodec<ExclusiveAttachmentStateArgs, ExclusiveAttachmentState> {
    return combineCodec(getExclusiveAttachmentStateEncoder(), getExclusiveAttachmentStateDecoder());
}

export function decodeExclusiveAttachmentState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<ExclusiveAttachmentState, TAddress>;
export function decodeExclusiveAttachmentState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<ExclusiveAttachmentState, TAddress>;
export function decodeExclusiveAttachmentState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<ExclusiveAttachmentState, TAddress> | MaybeAccount<ExclusiveAttachmentState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getExclusiveAttachmentStateDecoder());
}

export async function fetchExclusiveAttachmentState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ExclusiveAttachmentState, TAddress>> {
  const maybeAccount = await fetchMaybeExclusiveAttachmentState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveAttachmentState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ExclusiveAttachmentState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeExclusiveAttachmentState(maybeAccount);
}

export async function fetchAllExclusiveAttachmentState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ExclusiveAttachmentState>[]> {
  const maybeAccounts = await fetchAllMaybeExclusiveAttachmentState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeExclusiveAttachmentState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ExclusiveAttachmentState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeExclusiveAttachmentState(maybeAccount));
}

export async function fetchExclusiveAttachmentStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveAttachmentSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<ExclusiveAttachmentState>> {
  const maybeAccount = await fetchMaybeExclusiveAttachmentStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeExclusiveAttachmentStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: ExclusiveAttachmentSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<ExclusiveAttachmentState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findExclusiveAttachmentPda(seeds, { programAddress });
  return await fetchMaybeExclusiveAttachmentState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `ExclusiveAttachmentState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (exclusiveAttachmentStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ exclusiveAttachmentState: address, payer }).make());
 * }
 * ```
 */
export function exclusiveAttachmentStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 11) {
		return false;
	}
	return data[1]! < 0;
}
