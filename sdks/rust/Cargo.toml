[package]
name = "lootbox_sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
repository = "https://github.com/pina-rs/lootbox/tree/main/sdks/rust"
description = "Ergonomic Rust SDK for the Pina lootbox primitive"

[dependencies]
lootbox_program_client = { workspace = true }
solana-sha256-hasher = { workspace = true, features = ["sha2"] }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
