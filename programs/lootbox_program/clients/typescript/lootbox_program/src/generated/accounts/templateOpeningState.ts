/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixZeroPodEncoderSize, getZeroPodDiscriminatorDecoder } from "../zeropodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findTemplateOpeningPda, type TemplateOpeningSeeds } from '../pdas';

export const TEMPLATE_OPENING_STATE_DISCRIMINATOR = 6;

export function getTemplateOpeningStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(TEMPLATE_OPENING_STATE_DISCRIMINATOR); }

/** A burned box, its verified entropy, and independently claimable winning assets. */
export type TemplateOpeningState = { discriminator: number; template: Address;
/** Authority that owned and burned the box. */
boxAuthority: Address;
/** Immutable destination for every prize claim. */
beneficiary: Address;
/** Account that receives opening rent when the lifecycle is closed. */
rentRefund: Address;
/** Optional program expected to consume the result receipt. */
consumerProgram: Address;
/** Consumer-selected correlation key, fixed before randomness is known. */
consumerContext: ReadonlyUint8Array; randomness: Address; sequence: bigint; seedSlot: bigint; entropy: ReadonlyUint8Array;
/** Treasury revision and bundle prefix fixed before the box is burned. */
treasuryRevision: bigint; eligibleBundleCount: number;
/** 0 committed, 1 verified, 2 allocated, 3 delivered, 4 forfeited. */
status: number; selectedBundle: number; claimedMask: number; bump: number;  };

export type TemplateOpeningStateArgs = { template: Address;
/** Authority that owned and burned the box. */
boxAuthority: Address;
/** Immutable destination for every prize claim. */
beneficiary: Address;
/** Account that receives opening rent when the lifecycle is closed. */
rentRefund: Address;
/** Optional program expected to consume the result receipt. */
consumerProgram: Address;
/** Consumer-selected correlation key, fixed before randomness is known. */
consumerContext: ReadonlyUint8Array; randomness: Address; sequence: number | bigint; seedSlot: number | bigint; entropy: ReadonlyUint8Array;
/** Treasury revision and bundle prefix fixed before the box is burned. */
treasuryRevision: number | bigint; eligibleBundleCount: number;
/** 0 committed, 1 verified, 2 allocated, 3 delivered, 4 forfeited. */
status: number; selectedBundle: number; claimedMask: number; bump: number;  };

/** Gets the encoder for {@link TemplateOpeningStateArgs} account data. */
export function getTemplateOpeningStateEncoder(): FixedSizeEncoder<TemplateOpeningStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['template', getAddressEncoder()], ['boxAuthority', getAddressEncoder()], ['beneficiary', getAddressEncoder()], ['rentRefund', getAddressEncoder()], ['consumerProgram', getAddressEncoder()], ['consumerContext', fixZeroPodEncoderSize(getBytesEncoder(), 32)], ['randomness', getAddressEncoder()], ['sequence', getU64Encoder()], ['seedSlot', getU64Encoder()], ['entropy', fixZeroPodEncoderSize(getBytesEncoder(), 32)], ['treasuryRevision', getU64Encoder()], ['eligibleBundleCount', getU32Encoder()], ['status', getU8Encoder()], ['selectedBundle', getU32Encoder()], ['claimedMask', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 6 }));
}

/** Gets the decoder for {@link TemplateOpeningState} account data. */
export function getTemplateOpeningStateDecoder(): FixedSizeDecoder<TemplateOpeningState> {
    return getStructDecoder([['discriminator', getZeroPodDiscriminatorDecoder(TEMPLATE_OPENING_STATE_DISCRIMINATOR, getU8Decoder())], ['template', getAddressDecoder()], ['boxAuthority', getAddressDecoder()], ['beneficiary', getAddressDecoder()], ['rentRefund', getAddressDecoder()], ['consumerProgram', getAddressDecoder()], ['consumerContext', fixDecoderSize(getBytesDecoder(), 32)], ['randomness', getAddressDecoder()], ['sequence', getU64Decoder()], ['seedSlot', getU64Decoder()], ['entropy', fixDecoderSize(getBytesDecoder(), 32)], ['treasuryRevision', getU64Decoder()], ['eligibleBundleCount', getU32Decoder()], ['status', getU8Decoder()], ['selectedBundle', getU32Decoder()], ['claimedMask', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link TemplateOpeningState} account data. */
export function getTemplateOpeningStateCodec(): FixedSizeCodec<TemplateOpeningStateArgs, TemplateOpeningState> {
    return combineCodec(getTemplateOpeningStateEncoder(), getTemplateOpeningStateDecoder());
}

export function decodeTemplateOpeningState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<TemplateOpeningState, TAddress>;
export function decodeTemplateOpeningState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<TemplateOpeningState, TAddress>;
export function decodeTemplateOpeningState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<TemplateOpeningState, TAddress> | MaybeAccount<TemplateOpeningState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getTemplateOpeningStateDecoder());
}

export async function fetchTemplateOpeningState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TemplateOpeningState, TAddress>> {
  const maybeAccount = await fetchMaybeTemplateOpeningState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTemplateOpeningState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TemplateOpeningState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTemplateOpeningState(maybeAccount);
}

export async function fetchAllTemplateOpeningState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TemplateOpeningState>[]> {
  const maybeAccounts = await fetchAllMaybeTemplateOpeningState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTemplateOpeningState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TemplateOpeningState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTemplateOpeningState(maybeAccount));
}

export async function fetchTemplateOpeningStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TemplateOpeningSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<TemplateOpeningState>> {
  const maybeAccount = await fetchMaybeTemplateOpeningStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTemplateOpeningStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TemplateOpeningSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<TemplateOpeningState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findTemplateOpeningPda(seeds, { programAddress });
  return await fetchMaybeTemplateOpeningState(rpc, address, fetchConfig);
}