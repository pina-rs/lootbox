/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixZeroPodEncoderSize, getZeroPodBooleanDecoder, getZeroPodDiscriminatorDecoder } from "../zeropodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findLootboxPda, type LootboxSeeds } from '../pdas';

export const LOOTBOX_STATE_DISCRIMINATOR = 1;

export function getLootboxStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(LOOTBOX_STATE_DISCRIMINATOR); }

/** Immutable definition and live accounting for one lootbox mint. */
export type LootboxState = { discriminator: number; authority: Address; boxMint: Address; oracleProgram: Address; oracleQueue: Address; id: bigint; maxSupply: bigint; totalMinted: bigint; pendingOpenings: bigint; opened: bigint; refunded: bigint; totalWeight: bigint; maxRewardLamports: bigint;
/** Eight little-endian `u64` weight slots. */
outcomeWeights: ReadonlyUint8Array;
/** Eight little-endian `u64` reward slots. */
outcomeLamports: ReadonlyUint8Array; outcomeCount: number; sealed: boolean; bump: number; vaultBump: number;  };

export type LootboxStateArgs = { authority: Address; boxMint: Address; oracleProgram: Address; oracleQueue: Address; id: number | bigint; maxSupply: number | bigint; totalMinted: number | bigint; pendingOpenings: number | bigint; opened: number | bigint; refunded: number | bigint; totalWeight: number | bigint; maxRewardLamports: number | bigint;
/** Eight little-endian `u64` weight slots. */
outcomeWeights: ReadonlyUint8Array;
/** Eight little-endian `u64` reward slots. */
outcomeLamports: ReadonlyUint8Array; outcomeCount: number; sealed: boolean; bump: number; vaultBump: number;  };

/** Gets the encoder for {@link LootboxStateArgs} account data. */
export function getLootboxStateEncoder(): FixedSizeEncoder<LootboxStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['authority', getAddressEncoder()], ['boxMint', getAddressEncoder()], ['oracleProgram', getAddressEncoder()], ['oracleQueue', getAddressEncoder()], ['id', getU64Encoder()], ['maxSupply', getU64Encoder()], ['totalMinted', getU64Encoder()], ['pendingOpenings', getU64Encoder()], ['opened', getU64Encoder()], ['refunded', getU64Encoder()], ['totalWeight', getU64Encoder()], ['maxRewardLamports', getU64Encoder()], ['outcomeWeights', fixZeroPodEncoderSize(getBytesEncoder(), 64)], ['outcomeLamports', fixZeroPodEncoderSize(getBytesEncoder(), 64)], ['outcomeCount', getU8Encoder()], ['sealed', getBooleanEncoder()], ['bump', getU8Encoder()], ['vaultBump', getU8Encoder()]]), (value) => ({ ...value, discriminator: 1 }));
}

/** Gets the decoder for {@link LootboxState} account data. */
export function getLootboxStateDecoder(): FixedSizeDecoder<LootboxState> {
    return getStructDecoder([['discriminator', getZeroPodDiscriminatorDecoder(LOOTBOX_STATE_DISCRIMINATOR, getU8Decoder())], ['authority', getAddressDecoder()], ['boxMint', getAddressDecoder()], ['oracleProgram', getAddressDecoder()], ['oracleQueue', getAddressDecoder()], ['id', getU64Decoder()], ['maxSupply', getU64Decoder()], ['totalMinted', getU64Decoder()], ['pendingOpenings', getU64Decoder()], ['opened', getU64Decoder()], ['refunded', getU64Decoder()], ['totalWeight', getU64Decoder()], ['maxRewardLamports', getU64Decoder()], ['outcomeWeights', fixDecoderSize(getBytesDecoder(), 64)], ['outcomeLamports', fixDecoderSize(getBytesDecoder(), 64)], ['outcomeCount', getU8Decoder()], ['sealed', getZeroPodBooleanDecoder()], ['bump', getU8Decoder()], ['vaultBump', getU8Decoder()]]);
}

/** Gets the codec for {@link LootboxState} account data. */
export function getLootboxStateCodec(): FixedSizeCodec<LootboxStateArgs, LootboxState> {
    return combineCodec(getLootboxStateEncoder(), getLootboxStateDecoder());
}

export function decodeLootboxState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<LootboxState, TAddress>;
export function decodeLootboxState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<LootboxState, TAddress>;
export function decodeLootboxState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<LootboxState, TAddress> | MaybeAccount<LootboxState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLootboxStateDecoder());
}

export async function fetchLootboxState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LootboxState, TAddress>> {
  const maybeAccount = await fetchMaybeLootboxState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLootboxState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LootboxState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLootboxState(maybeAccount);
}

export async function fetchAllLootboxState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LootboxState>[]> {
  const maybeAccounts = await fetchAllMaybeLootboxState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLootboxState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LootboxState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLootboxState(maybeAccount));
}

export async function fetchLootboxStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: LootboxSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<LootboxState>> {
  const maybeAccount = await fetchMaybeLootboxStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLootboxStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: LootboxSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<LootboxState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findLootboxPda(seeds, { programAddress });
  return await fetchMaybeLootboxState(rpc, address, fetchConfig);
}