//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Hands an empty badge mint's authority to a funding bundle for mint-on-claim
/// delivery.
///
/// The template authority, which must be the mint's current mint authority,
/// signs. The mint needs zero supply and decimals, no freeze authority, and
/// immutable metadata if any. Mint authority moves to the bundle PDA and one
/// badge is recorded per win.
/// CPI call for the `fund_mint_prize` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct FundMintPrize<'account> {
	/// CPI account `authority`.
	/// Template authority and current mint authority of `mint`; signs the
	/// authority handoff.
	/// Required privileges: read-only and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA that must be unlocked and not retired; read only by the
	/// handler.
	/// Required privileges: writable.
	pub template: &'account AccountView,

	/// CPI account `bundle`.
	/// Funding bundle PDA of this template that becomes the mint authority.
	/// Required privileges: writable.
	pub bundle: &'account AccountView,

	/// CPI account `mint`.
	/// Empty zero-decimal badge mint with no freeze authority; its mint
	/// authority changes here.
	/// Required privileges: writable.
	pub mint: &'account AccountView,

	/// CPI account `tokenProgram`.
	/// SPL Token or Token-2022 program that owns `mint`, invoked to set the
	/// authority.
	/// Required privileges: read-only.
	pub token_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `fund_mint_prize`.
	pub ix: FundMintPrizeIx,
}

/// Instruction arguments for the `fund_mint_prize` CPI call.
#[derive(Clone, Copy, Debug, Default)]
pub struct FundMintPrizeIx;

impl FundMintPrizeIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 2;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 2], ProgramError> {
		let mut data = [0u8; 2];
		data[..2].copy_from_slice(&FUND_MINT_PRIZE_DISCRIMINATOR);

		Ok(data)
	}
}

impl<'account> FundMintPrize<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::readonly_signer(self.authority),
			CpiHandle::writable(self.template)?,
			CpiHandle::writable(self.bundle)?,
			CpiHandle::writable(self.mint)?,
			CpiHandle::readonly(self.token_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const FUND_MINT_PRIZE_DISCRIMINATOR: [u8; 2] = [41, 0];
