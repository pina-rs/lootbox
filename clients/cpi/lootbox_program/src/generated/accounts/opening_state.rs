//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::Address;

#[allow(unused_imports)]
use crate::generated_types::*;

/// Receipt binding a burned box to one unrevealed randomness commitment.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct OpeningState {
	pub lootbox: Address,
	pub recipient: Address,
	pub randomness: Address,
	pub seed_slot: u64,
	pub reward_lamports: u64,
	pub selected_outcome: u8,
	pub status: u8,
	pub bump: u8,
}

/// Account discriminator declared by the program's IDL.
pub const OPENING_STATE_DISCRIMINATOR: [u8; 2] = [3, 0];

impl OpeningState {
	/// Encoded size of this account's data.
	pub const LEN: usize = 117;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 2
			&& data[..2] == OPENING_STATE_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<OpeningState> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 2;

		let lootbox = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let recipient = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let randomness = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let seed_slot: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let reward_lamports: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let selected_outcome: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let status: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);

		Some(OpeningState {
			lootbox,
			recipient,
			randomness,
			seed_slot,
			reward_lamports,
			selected_outcome,
			status,
			bump,
		})
	}
}
