[package]
name = "switchboard_randomness_cpi"
version = "0.0.2"
edition.workspace = true
license.workspace = true
publish = false
repository = "https://github.com/pina-rs/lootbox/tree/main/crates/switchboard_randomness_cpi"
description = "Pinocchio CPI helpers for the Switchboard On-Demand randomness program"

[features]
# Links `std` for host-side consumers; on-chain builds stay `no_std`.
std = []

[dependencies]
pinocchio = { workspace = true }
solana-address = { workspace = true }

[lints]
workspace = true
