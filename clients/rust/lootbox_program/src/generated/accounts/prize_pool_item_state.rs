//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct PrizePoolItemState {
/// Immutable identity and normalized metadata commitment for one Bubblegum leaf.
///
/// Bubblegum authorities may change verification flags after custody, so claims
/// re-prove the current metadata while pinning every semantic field.
	pub discriminator: u8,
	pub migration_version: u8,
	pub pool: solana_pubkey::Pubkey,
	pub asset: solana_pubkey::Pubkey,
	pub data_hash: [u8; 32],
	pub creator_hash: [u8; 32],
	pub semantic_metadata_hash: [u8; 32],
	/// Restores the append-only accumulator when an unfinished tail is removed.
	pub previous_manifest_accumulator: [u8; 32],
	pub nonce: u64,
	pub tree_index: u32,
	pub pool_index: u32,
	/// 0 metadata-admitted, 1 transferred into `PrizePool` custody.
	pub status: u8,
	pub bump: u8,
}

pub const PRIZE_POOL_ITEM_STATE_DISCRIMINATOR: u8 = 9u8;

pub const PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION: u8 = 0u8;

impl PrizePoolItemState {
	pub const LEN: usize = core::mem::size_of::<PrizePoolItemStateZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut PrizePoolItemStateZc),
	) -> Result<&mut PrizePoolItemStateZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = PRIZE_POOL_ITEM_STATE_DISCRIMINATOR;
			account.migration_version = PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(data: &[u8]) -> Result<&PrizePoolItemStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != PRIZE_POOL_ITEM_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(data: &mut [u8]) -> Result<&mut PrizePoolItemStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != PRIZE_POOL_ITEM_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl PrizePoolItemState {
	pub fn find_pda(pool: &solana_pubkey::Pubkey, pool_index: u32) -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&[
				"prize-pool-item".as_bytes(),
				pool.as_ref(),
				&pool_index.to_le_bytes(),
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}

	pub fn create_pda(pool: &solana_pubkey::Pubkey, pool_index: u32, bump: u8) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&[
				"prize-pool-item".as_bytes(),
				pool.as_ref(),
				&pool_index.to_le_bytes(),
				&[bump],
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}
}


/// Whether raw account bytes are stale for this contract: the envelope names this account's discriminator and carries a version older than
/// [`PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION`]. Current or foreign bytes return false; decoding explains the difference.
///
/// Version 0 is the initial version, so no bytes can ever be stale.
pub fn prize_pool_item_state_needs_migration(_data: &[u8]) -> bool {
	false
}


/// Why `PrizePoolItemState::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum PrizePoolItemStateVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version predates this client: migrate the account on-chain, then retry.
	Stale { stored: u8 },
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for PrizePoolItemStateVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid PrizePoolItemState account data"),
			Self::Stale { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data predates this client; migrate it by sending a transaction to the program, or decode it with a client generated from an older IDL)"
			),
			Self::Future { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data was written by a newer program; upgrade this client)"
			),
		}
	}
}

impl PrizePoolItemState {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`PrizePoolItemState::from_bytes`].
	pub fn try_from_bytes(
		data: &[u8],
	) -> Result<&PrizePoolItemStateZc, PrizePoolItemStateVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| PrizePoolItemStateVersionError::InvalidData)?;
		if account.discriminator != PRIZE_POOL_ITEM_STATE_DISCRIMINATOR {
			return Err(PrizePoolItemStateVersionError::InvalidData);
		}
		if account.migration_version > PRIZE_POOL_ITEM_STATE_MIGRATION_VERSION {
			return Err(PrizePoolItemStateVersionError::Future { stored: account.migration_version });
		}
		Ok(account)
	}
}

#[cfg(test)]
mod prize_pool_item_state_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<PrizePoolItemStateZc>()];
		data[..1].copy_from_slice(&[9]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let error = PrizePoolItemState::try_from_bytes(&envelope(1_u8)).err().expect("a future envelope must fail");
		assert_eq!(error, PrizePoolItemStateVersionError::Future { stored: 1 });
		assert_eq!(PrizePoolItemStateVersionError::Future { stored: 1 }.to_string(), "migration version mismatch: expected 0, received 1 (the data was written by a newer program; upgrade this client)");
		assert!(
			PrizePoolItemState::try_from_bytes(&envelope(0_u8)).is_ok(),
			"the current version must decode",
		);
	}
}
