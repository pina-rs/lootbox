/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 *
 * The framework-owned `Migrate` instruction: it runs the program's on-demand
 * account migrations on their own, so the payer authorizes exactly the
 * migration cost and the business instruction that follows sees current
 * data. The intended flow around any instruction that failed with a
 * migration version mismatch is catch -> migrate -> retry.
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount,
} from "@solana/kit";
import {
	getAccountMetaFactory,
	type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LOOTBOX_PROGRAM_PROGRAM_ADDRESS } from "../programs";

/** Discriminator reserved by Pina for the framework `Migrate` instruction. */
export const MIGRATE_DISCRIMINATOR = 255;

export function getMigrateDiscriminatorBytes(): ReadonlyUint8Array {
	return new Uint8Array([255]);
}

export type MigrateInstruction<
	TProgram extends string = typeof LOOTBOX_PROGRAM_PROGRAM_ADDRESS,
	TAccountPayer extends string | AccountMeta<string> = string,
	TAccountSystemProgram extends string | AccountMeta<string> = string,
	TAccountLootboxState extends string | AccountMeta<string> = string,
	TAccountVaultState extends string | AccountMeta<string> = string,
	TAccountOpeningState extends string | AccountMeta<string> = string,
	TAccountTemplateState extends string | AccountMeta<string> = string,
	TAccountBundleState extends string | AccountMeta<string> = string,
	TAccountTemplateOpeningState extends string | AccountMeta<string> = string,
	TAccountResultReceiptState extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = []
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountPayer extends string ?
					& WritableSignerAccount<TAccountPayer>
					& AccountSignerMeta<TAccountPayer>
				: TAccountPayer,
			TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram>
				: TAccountSystemProgram,
			TAccountLootboxState extends string ? WritableAccount<TAccountLootboxState>
				: TAccountLootboxState,
			TAccountVaultState extends string ? WritableAccount<TAccountVaultState>
				: TAccountVaultState,
			TAccountOpeningState extends string ? WritableAccount<TAccountOpeningState>
				: TAccountOpeningState,
			TAccountTemplateState extends string ? WritableAccount<TAccountTemplateState>
				: TAccountTemplateState,
			TAccountBundleState extends string ? WritableAccount<TAccountBundleState>
				: TAccountBundleState,
			TAccountTemplateOpeningState extends string ? WritableAccount<TAccountTemplateOpeningState>
				: TAccountTemplateOpeningState,
			TAccountResultReceiptState extends string ? WritableAccount<TAccountResultReceiptState>
				: TAccountResultReceiptState,
			...TRemainingAccounts,
		]
	>;

export type MigrateInput<
	TAccountPayer extends string = string,
	TAccountSystemProgram extends string = string,
	TAccountLootboxState extends string = string,
	TAccountVaultState extends string = string,
	TAccountOpeningState extends string = string,
	TAccountTemplateState extends string = string,
	TAccountBundleState extends string = string,
	TAccountTemplateOpeningState extends string = string,
	TAccountResultReceiptState extends string = string
> = {
	payer?: TransactionSigner<TAccountPayer>;
	systemProgram?: Address<TAccountSystemProgram>;
	lootboxState?: Address<TAccountLootboxState>;
	vaultState?: Address<TAccountVaultState>;
	openingState?: Address<TAccountOpeningState>;
	templateState?: Address<TAccountTemplateState>;
	bundleState?: Address<TAccountBundleState>;
	templateOpeningState?: Address<TAccountTemplateOpeningState>;
	resultReceiptState?: Address<TAccountResultReceiptState>;
};

/**
 * Composes the reserved `Migrate` instruction.
 *
 * Every migratable account is optional: omitted slots are filled with the
 * program-address placeholder and trailing omitted slots are dropped, so a
 * client sends only the accounts it needs to migrate. The `payer` funds rent
 * deficits and must be a writable signer; omit it when no migration needs
 * funding. The program caps the whole instruction's rent transfers at a
 * program-chosen lamport budget.
 */
export function getMigrateInstruction<
	TAccountPayer extends string = string,
	TAccountSystemProgram extends string = string,
	TAccountLootboxState extends string = string,
	TAccountVaultState extends string = string,
	TAccountOpeningState extends string = string,
	TAccountTemplateState extends string = string,
	TAccountBundleState extends string = string,
	TAccountTemplateOpeningState extends string = string,
	TAccountResultReceiptState extends string = string,
	TProgramAddress extends Address = typeof LOOTBOX_PROGRAM_PROGRAM_ADDRESS
>(
	input: MigrateInput<
		TAccountPayer,
		TAccountSystemProgram,
		TAccountLootboxState,
		TAccountVaultState,
		TAccountOpeningState,
		TAccountTemplateState,
		TAccountBundleState,
		TAccountTemplateOpeningState,
		TAccountResultReceiptState
	>,
	config?: { programAddress?: TProgramAddress },
): MigrateInstruction<
	TProgramAddress,
	TAccountPayer,
	TAccountSystemProgram,
	TAccountLootboxState,
	TAccountVaultState,
	TAccountOpeningState,
	TAccountTemplateState,
	TAccountBundleState,
	TAccountTemplateOpeningState,
	TAccountResultReceiptState
> {
	// Program address.
	const programAddress = config?.programAddress ??
		LOOTBOX_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		payer: { value: input.payer ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false },
		lootboxState: { value: input.lootboxState ?? null, isWritable: true },
		vaultState: { value: input.vaultState ?? null, isWritable: true },
		openingState: { value: input.openingState ?? null, isWritable: true },
		templateState: { value: input.templateState ?? null, isWritable: true },
		bundleState: { value: input.bundleState ?? null, isWritable: true },
		templateOpeningState: { value: input.templateOpeningState ?? null, isWritable: true },
		resultReceiptState: { value: input.resultReceiptState ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;
	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

	// Slots after the last provided account may be truncated: the program
	// treats a missing trailing slot exactly like the program-address
	// placeholder.
	const provided = [input.payer, input.systemProgram, input.lootboxState, input.vaultState, input.openingState, input.templateState, input.bundleState, input.templateOpeningState, input.resultReceiptState];
	let lastProvided = -1;
	for (let index = 0; index < provided.length; index += 1) {
		if (provided[index] != null) {
			lastProvided = index;
		}
	}

	return Object.freeze({
		accounts: [
		getAccountMeta("payer", accounts.payer),
		getAccountMeta("systemProgram", accounts.systemProgram),
		getAccountMeta("lootboxState", accounts.lootboxState),
		getAccountMeta("vaultState", accounts.vaultState),
		getAccountMeta("openingState", accounts.openingState),
		getAccountMeta("templateState", accounts.templateState),
		getAccountMeta("bundleState", accounts.bundleState),
		getAccountMeta("templateOpeningState", accounts.templateOpeningState),
		getAccountMeta("resultReceiptState", accounts.resultReceiptState),
		].slice(0, lastProvided + 1),
		data: getMigrateDiscriminatorBytes(),
		programAddress,
	} as MigrateInstruction<
		TProgramAddress,
	TAccountPayer,
	TAccountSystemProgram,
	TAccountLootboxState,
	TAccountVaultState,
	TAccountOpeningState,
	TAccountTemplateState,
	TAccountBundleState,
	TAccountTemplateOpeningState,
	TAccountResultReceiptState
	>);
}
