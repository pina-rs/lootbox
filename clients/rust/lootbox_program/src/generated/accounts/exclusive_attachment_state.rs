//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct ExclusiveAttachmentState {
/// One bundle slot's promise of Exclusive Lootbox NFTs from a collection.
///
/// The PDA commits the slot to the collection's frozen layers and owns a
/// zero-data fee vault prepaying Bubblegum's per-mint fee, so a claim costs
/// its submitter only the transaction fee.
	pub discriminator: u8,
	pub migration_version: u8,
	/// Template PDA whose bundle attached; set at attach time.
	pub template: solana_pubkey::Pubkey,
	/// Bundle PDA whose slot this attachment fills; part of the PDA seeds.
	pub bundle: solana_pubkey::Pubkey,
	/// `ExclusiveCollectionState` PDA that every claim mints from.
	pub collection: solana_pubkey::Pubkey,
	/// The collection's frozen `layers_hash` at attach time.
	pub layers_hash: [u8; 32],
	/// Copies the bound bundle slot can ever mint.
	///
	/// Copied from the bundle's quantity at attach time.
	pub quantity: u64,
	/// Copies minted through this attachment.
	///
	/// Incremented on each claim and never exceeds `quantity`.
	pub minted: u64,
	/// Bubblegum mint fee escrowed per copy.
	///
	/// Lamports; fixed at attach time to Bubblegum V2's 90,000-lamport fee.
	pub mint_fee_lamports: u64,
	/// Bundle manifest slot bound to the collection; part of the PDA seeds.
	pub asset_index: u8,
	/// Canonical bump of this PDA `["exclusive-attachment", bundle, asset_index]`.
	pub bump: u8,
	/// Canonical bump of the fee vault PDA `["exclusive-fee-vault", attachment]`.
	pub fee_vault_bump: u8,
}

pub const EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR: u8 = 11u8;

pub const EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION: u8 = 0u8;

impl ExclusiveAttachmentState {
	pub const LEN: usize = core::mem::size_of::<ExclusiveAttachmentStateZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut ExclusiveAttachmentStateZc),
	) -> Result<&mut ExclusiveAttachmentStateZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR;
			account.migration_version = EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(data: &[u8]) -> Result<&ExclusiveAttachmentStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(data: &mut [u8]) -> Result<&mut ExclusiveAttachmentStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl ExclusiveAttachmentState {
	pub fn find_pda(bundle: &solana_pubkey::Pubkey, asset_index: u8) -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&[
				"exclusive-attachment".as_bytes(),
				bundle.as_ref(),
				&asset_index.to_le_bytes(),
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}

	pub fn create_pda(bundle: &solana_pubkey::Pubkey, asset_index: u8, bump: u8) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&[
				"exclusive-attachment".as_bytes(),
				bundle.as_ref(),
				&asset_index.to_le_bytes(),
				&[bump],
			],
			&crate::LOOTBOX_PROGRAM_ID,
		)
	}
}


/// Whether raw account bytes are stale for this contract: the envelope names this account's discriminator and carries a version older than
/// [`EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION`]. Current or foreign bytes return false; decoding explains the difference.
///
/// Version 0 is the initial version, so no bytes can ever be stale.
pub fn exclusive_attachment_state_needs_migration(_data: &[u8]) -> bool {
	false
}


/// Why `ExclusiveAttachmentState::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ExclusiveAttachmentStateVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version predates this client: migrate the account on-chain, then retry.
	Stale { stored: u8 },
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for ExclusiveAttachmentStateVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid ExclusiveAttachmentState account data"),
			Self::Stale { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data predates this client; migrate it by sending a transaction to the program, or decode it with a client generated from an older IDL)"
			),
			Self::Future { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data was written by a newer program; upgrade this client)"
			),
		}
	}
}

impl ExclusiveAttachmentState {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`ExclusiveAttachmentState::from_bytes`].
	pub fn try_from_bytes(
		data: &[u8],
	) -> Result<&ExclusiveAttachmentStateZc, ExclusiveAttachmentStateVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| ExclusiveAttachmentStateVersionError::InvalidData)?;
		if account.discriminator != EXCLUSIVE_ATTACHMENT_STATE_DISCRIMINATOR {
			return Err(ExclusiveAttachmentStateVersionError::InvalidData);
		}
		if account.migration_version > EXCLUSIVE_ATTACHMENT_STATE_MIGRATION_VERSION {
			return Err(ExclusiveAttachmentStateVersionError::Future { stored: account.migration_version });
		}
		Ok(account)
	}
}

#[cfg(test)]
mod exclusive_attachment_state_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<ExclusiveAttachmentStateZc>()];
		data[..1].copy_from_slice(&[11]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let error = ExclusiveAttachmentState::try_from_bytes(&envelope(1_u8)).err().expect("a future envelope must fail");
		assert_eq!(error, ExclusiveAttachmentStateVersionError::Future { stored: 1 });
		assert_eq!(ExclusiveAttachmentStateVersionError::Future { stored: 1 }.to_string(), "migration version mismatch: expected 0, received 1 (the data was written by a newer program; upgrade this client)");
		assert!(
			ExclusiveAttachmentState::try_from_bytes(&envelope(0_u8)).is_ok(),
			"the current version must decode",
		);
	}
}
