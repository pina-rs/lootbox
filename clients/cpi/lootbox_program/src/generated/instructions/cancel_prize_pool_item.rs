//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Close the pool's prepared item before its leaf is transferred.
///
/// The template authority signs while the treasury is unlocked and the bundle
/// is funding. The pool must be funding with a prepared item at
/// `deposit_cursor`. Clears `has_prepared_item` and closes the item PDA,
/// returning its rent to the authority; deposited items are never touched.
/// CPI call for the `cancel_prize_pool_item` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct CancelPrizePoolItem<'account> {
	/// CPI account `authority`.
	/// Template authority; signs and receives the closed item's rent.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA; its treasury must be unlocked.
	/// Required privileges: read-only.
	pub template: &'account AccountView,

	/// CPI account `bundle`.
	/// Funding bundle PDA of `template`.
	/// Required privileges: read-only.
	pub bundle: &'account AccountView,

	/// CPI account `prizePool`.
	/// Funding `PrizePoolState` PDA; `has_prepared_item` is cleared.
	/// Required privileges: writable.
	pub prize_pool: &'account AccountView,

	/// CPI account `prizePoolItem`.
	/// Prepared item PDA at the pool's `deposit_cursor`, closed here.
	/// Required privileges: writable.
	pub prize_pool_item: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `cancel_prize_pool_item`.
	pub ix: CancelPrizePoolItemIx,
}

/// Instruction arguments for the `cancel_prize_pool_item` CPI call.
#[derive(Clone, Copy, Debug, Default)]
pub struct CancelPrizePoolItemIx;

impl CancelPrizePoolItemIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 2;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 2], ProgramError> {
		let mut data = [0u8; 2];
		data[..2].copy_from_slice(&CANCEL_PRIZE_POOL_ITEM_DISCRIMINATOR);

		Ok(data)
	}
}

impl<'account> CancelPrizePoolItem<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.template),
			CpiHandle::readonly(self.bundle),
			CpiHandle::writable(self.prize_pool)?,
			CpiHandle::writable(self.prize_pool_item)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CANCEL_PRIZE_POOL_ITEM_DISCRIMINATOR: [u8; 2] = [52, 0];
