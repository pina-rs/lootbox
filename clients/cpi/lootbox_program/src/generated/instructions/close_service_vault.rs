//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Returns a retired, fully settled template's service vault balance to its
/// authority.
///
/// The template authority signs. The template must have receipts or bounties
/// configured, be retired, have no pending openings, and have zero box supply.
/// The whole balance, including unused prepaid service funds and the rent
/// reserve, moves to the authority; an empty vault is a no-op.
/// CPI call for the `close_service_vault` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct CloseServiceVault<'account> {
	/// CPI account `authority`.
	/// Template authority; signs and receives the vault balance.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Retired template PDA with no pending openings.
	/// Required privileges: read-only.
	pub template: &'account AccountView,

	/// CPI account `boxMint`.
	/// Template's box mint; its supply must be zero.
	/// Required privileges: read-only.
	pub box_mint: &'account AccountView,

	/// CPI account `serviceVault`.
	/// System-owned service vault PDA from `["service-vault", template]` with
	/// the template's stored bump; signs the transfer out.
	/// Required privileges: writable.
	pub service_vault: &'account AccountView,

	/// CPI account `systemProgram`.
	/// System program, invoked for the lamport transfer.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `close_service_vault`.
	pub ix: CloseServiceVaultIx,
}

/// Instruction arguments for the `close_service_vault` CPI call.
#[derive(Clone, Copy, Debug, Default)]
pub struct CloseServiceVaultIx;

impl CloseServiceVaultIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 2;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 2], ProgramError> {
		let mut data = [0u8; 2];
		data[..2].copy_from_slice(&CLOSE_SERVICE_VAULT_DISCRIMINATOR);

		Ok(data)
	}
}

impl<'account> CloseServiceVault<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.template),
			CpiHandle::readonly(self.box_mint),
			CpiHandle::writable(self.service_vault)?,
			CpiHandle::readonly(self.system_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CLOSE_SERVICE_VAULT_DISCRIMINATOR: [u8; 2] = [38, 0];
