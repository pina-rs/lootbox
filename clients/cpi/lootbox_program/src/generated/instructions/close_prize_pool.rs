//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Close a prize pool that holds no outstanding leaves.
///
/// The template authority signs. Accepts an empty unsealed pool, a sealed pool
/// in a funding bundle whose every item was reclaimed, or a sealed pool in a
/// retired active bundle whose every item was claimed or reclaimed. The first
/// two require an unlocked treasury and clear the bundle slot. The pool
/// account closes and its rent returns to the authority.
/// CPI call for the `close_prize_pool` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ClosePrizePool<'account> {
	/// CPI account `authority`.
	/// Template authority; signs and receives the pool rent.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA that owns `bundle`.
	/// Required privileges: read-only.
	pub template: &'account AccountView,

	/// CPI account `bundle`.
	/// Bundle PDA; its pool slot is cleared unless the pool is terminal in an
	/// active bundle.
	/// Required privileges: writable.
	pub bundle: &'account AccountView,

	/// CPI account `prizePool`.
	/// `PrizePoolState` PDA, closed here.
	/// Required privileges: writable.
	pub prize_pool: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `close_prize_pool`.
	pub ix: ClosePrizePoolIx,
}

/// Instruction arguments for the `close_prize_pool` CPI call.
#[derive(Clone, Copy, Debug, Default)]
pub struct ClosePrizePoolIx;

impl ClosePrizePoolIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 2;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 2], ProgramError> {
		let mut data = [0u8; 2];
		data[..2].copy_from_slice(&CLOSE_PRIZE_POOL_DISCRIMINATOR);

		Ok(data)
	}
}

impl<'account> ClosePrizePool<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 4] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::readonly(self.template),
			CpiHandle::writable(self.bundle)?,
			CpiHandle::writable(self.prize_pool)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CLOSE_PRIZE_POOL_DISCRIMINATOR: [u8; 2] = [50, 0];
