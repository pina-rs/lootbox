//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::Address;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramResult;
use pina::ProgramError;
use pina::Signer;

use crate::ProgramAccount;

/// Creates a draft treasury template PDA bound to an empty Token-2022 box mint.
///
/// The creator signs and pays rent. The box mint must have zero supply and
/// decimals, no freeze authority, this template PDA as mint authority, and
/// immutable on-mint metadata whose name and URI match the arguments.
/// CPI call for the `create_template` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct CreateTemplate<'account, 'argument> {
	/// CPI account `authority`.
	/// Creator; signs, pays the template rent, and becomes its authority.
	/// Required privileges: writable and signer.
	pub authority: &'account AccountView,

	/// CPI account `template`.
	/// Template PDA created here from `["template", authority, id]`; must be
	/// empty.
	/// Required privileges: writable.
	pub template: &'account AccountView,

	/// CPI account `boxMint`.
	/// Empty Token-2022 box mint whose mint authority is the template PDA and
	/// whose metadata pointer and immutable metadata point at itself.
	/// Required privileges: read-only.
	pub box_mint: &'account AccountView,

	/// CPI account `systemProgram`.
	/// System program, invoked to create the template account.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// CPI account `boxTokenProgram`.
	/// Token-2022 program that must own the box mint.
	/// Required privileges: read-only.
	pub box_token_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `create_template`.
	pub ix: CreateTemplateIx<'argument>,
}

/// Instruction arguments for the `create_template` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct CreateTemplateIx<'argument> {
	/// Instruction argument `id`.
	pub id: u64,

	/// Instruction argument `opensAt`.
	pub opens_at: i64,

	/// Instruction argument `oracleProgram`.
	pub oracle_program: &'argument Address,

	/// Instruction argument `oracleQueue`.
	pub oracle_queue: &'argument Address,

	/// Instruction argument `name`.
	pub name: [u8; 32],

	/// Instruction argument `uri`.
	pub uri: [u8; 200],

	/// Instruction argument `settlementBountyLamports`.
	pub settlement_bounty_lamports: u64,

	/// Instruction argument `resultReceiptsEnabled`.
	pub result_receipts_enabled: bool,

	/// Instruction argument `bump`.
	pub bump: u8,
}

impl<'argument> CreateTemplateIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 324;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 324], ProgramError> {
		let mut data = [0u8; 324];
		data[..2].copy_from_slice(&CREATE_TEMPLATE_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.id.to_le_bytes());
		data[10..18].copy_from_slice(&self.opens_at.to_le_bytes());
		data[18..50].copy_from_slice(self.oracle_program.as_ref());
		data[50..82].copy_from_slice(self.oracle_queue.as_ref());
		data[82..114].copy_from_slice(&self.name);
		data[114..314].copy_from_slice(&self.uri);
		data[314..322].copy_from_slice(&self.settlement_bounty_lamports.to_le_bytes());
		data[322] = u8::from(self.result_receipts_enabled);
		data[323..324].copy_from_slice(&self.bump.to_le_bytes());

		Ok(data)
	}
}

impl<'account, 'argument> CreateTemplate<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 5] = [
			CpiHandle::writable_signer(self.authority)?,
			CpiHandle::writable(self.template)?,
			CpiHandle::readonly(self.box_mint),
			CpiHandle::readonly(self.system_program),
			CpiHandle::readonly(self.box_token_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CREATE_TEMPLATE_DISCRIMINATOR: [u8; 2] = [10, 0];
