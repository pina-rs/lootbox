//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

#![allow(rustdoc::broken_intra_doc_links)]

use pina::Address;

#[allow(unused_imports)]
use crate::generated_types::*;

/// Immutable rarity rules, art references, bonus reserves, and mint counters of
/// one template's Exclusive Lootbox NFT series.
///
/// The PDA is the Bubblegum tree creator and the Core collection update
/// authority. Its lamports hold rent plus every unpaid tier bonus:
/// `lamports >= rent_reserve + sum(bonus_remaining[k] * bonus_lamports[k])`.
/// A separate zero-data fee vault PDA prepays Bubblegum's per-mint fee, so a
/// claim costs its submitter only the transaction fee.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct ExclusiveSeriesState {
	pub authority: Address,
	pub template: Address,
	pub bundle: Address,
	pub collection: Address,
	pub merkle_tree: Address,
	pub quantity: u64,
	pub minted: u64,
	pub rent_reserve: u64,
	pub mint_fee_lamports: u64,
	pub weights: [u8; 64],
	pub bonus_lamports: [u8; 128],
	pub bonus_counts: [u8; 64],
	pub bonus_remaining: [u8; 64],
	pub tier_minted: [u8; 64],
	pub name_prefix: [u8; 32],
	pub symbol: [u8; 10],
	pub base_uri: [u8; 96],
	pub max_buffer_size: u32,
	pub asset_index: u8,
	pub contents_count: u8,
	pub background_count: u8,
	pub pattern_count: u8,
	pub max_depth: u8,
	pub status: u8,
	pub bump: u8,
	pub fee_vault_bump: u8,
}

/// Account discriminator declared by the program's IDL.
pub const EXCLUSIVE_SERIES_STATE_DISCRIMINATOR: [u8; 2] = [10, 0];

impl ExclusiveSeriesState {
	/// Encoded size of this account's data.
	pub const LEN: usize = 728;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 2
			&& data[..2] == EXCLUSIVE_SERIES_STATE_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<ExclusiveSeriesState> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 2;

		let authority = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let template = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let bundle = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let collection = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let merkle_tree = Address::new_from_array(data.get(cursor..cursor + 32)?
		.try_into().ok()?);
		cursor += 32;
		let quantity: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let minted: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let rent_reserve: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let mint_fee_lamports: u64 = u64::from_le_bytes(data.get(cursor..cursor + 8)?
		.try_into().ok()?);
		cursor += 8;
		let weights: [u8; 64] = data.get(cursor..cursor + 64)?
		.try_into().ok()?;
		cursor += 64;
		let bonus_lamports: [u8; 128] = data.get(cursor..cursor + 128)?
		.try_into().ok()?;
		cursor += 128;
		let bonus_counts: [u8; 64] = data.get(cursor..cursor + 64)?
		.try_into().ok()?;
		cursor += 64;
		let bonus_remaining: [u8; 64] = data.get(cursor..cursor + 64)?
		.try_into().ok()?;
		cursor += 64;
		let tier_minted: [u8; 64] = data.get(cursor..cursor + 64)?
		.try_into().ok()?;
		cursor += 64;
		let name_prefix: [u8; 32] = data.get(cursor..cursor + 32)?
		.try_into().ok()?;
		cursor += 32;
		let symbol: [u8; 10] = data.get(cursor..cursor + 10)?
		.try_into().ok()?;
		cursor += 10;
		let base_uri: [u8; 96] = data.get(cursor..cursor + 96)?
		.try_into().ok()?;
		cursor += 96;
		let max_buffer_size: u32 = u32::from_le_bytes(data.get(cursor..cursor + 4)?
		.try_into().ok()?);
		cursor += 4;
		let asset_index: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let contents_count: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let background_count: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let pattern_count: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let max_depth: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let status: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);
		cursor += 1;
		let fee_vault_bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?
		.try_into().ok()?);

		Some(ExclusiveSeriesState {
			authority,
			template,
			bundle,
			collection,
			merkle_tree,
			quantity,
			minted,
			rent_reserve,
			mint_fee_lamports,
			weights,
			bonus_lamports,
			bonus_counts,
			bonus_remaining,
			tier_minted,
			name_prefix,
			symbol,
			base_uri,
			max_buffer_size,
			asset_index,
			contents_count,
			background_count,
			pattern_count,
			max_depth,
			status,
			bump,
			fee_vault_bump,
		})
	}
}
