/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { fixPinaPodEncoderSize, getPinaPodBoundedArrayDecoder, getPinaPodBoundedArrayEncoder, getPinaPodBoundedCountDecoder, getPinaPodBooleanDecoder, getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { findPrizePoolPda, type PrizePoolSeeds } from '../pdas';

export const PRIZE_POOL_STATE_DISCRIMINATOR = 8;

export function getPrizePoolStateDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(PRIZE_POOL_STATE_DISCRIMINATOR); }

export const PRIZE_POOL_STATE_DISCRIMINATOR2 = 0;

export function getPrizePoolStateDiscriminator2Bytes(): ReadonlyUint8Array { return getU8Encoder().encode(PRIZE_POOL_STATE_DISCRIMINATOR2); }

/**
 * Compact, append-only Bubblegum inventory for one bundle manifest slot.
 *
 * The account pays only for deposited bitmap bytes: one byte for every eight
 * items. Per-item PDAs retain deposit snapshots while the sealed accumulator
 * commits the ordered inventory into the parent treasury manifest.
 */
export type PrizePoolState = { discriminator: number; migrationVersion: number;
/**
 * Template authority that created the pool. Receives item rent when a
 * winner claims an item.
 */
authority: Address;
/** Bundle whose asset slot this pool fills; seeds this PDA. */
bundle: Address;
/** Nonzero Bubblegum Merkle tree that holds every deposited leaf. */
tree: Address;
/**
 * Chained hash of every deposited item in pool order. Removing the
 * unsealed tail restores the previous value; sealing commits it into the
 * bundle slot.
 */
manifestAccumulator: ReadonlyUint8Array;
/**
 * Items the pool must hold before sealing; equals the bundle quantity and
 * is between one and 4,096.
 */
quantity: bigint;
/**
 * Seal counter mixed into the bundle slot commitment; `sealPrizePool`
 * increments it from zero.
 */
version: bigint;
/** Items deposited so far, and the pool index of the next item. */
depositCursor: number;
/** Reserved items, including those already claimed by winners. */
assignedCount: number;
/** Assigned items already delivered to their winners. */
claimedCount: number;
/** Items returned to the creator from the sealed pool. */
reclaimedCount: number;
/** Bundle asset slot this pool fills; seeds this PDA. */
assetIndex: number;
/** 0 funding, 1 sealed. */
status: number;
/** One metadata-admitted item PDA exists at `deposit_cursor`. */
hasPreparedItem: boolean;
/** Canonical bump of this prize pool PDA. */
bump: number;
/**
 * A set bit means the item cannot be allocated again.
 * Holds one bit per deposited item, so its length is `deposit_cursor`
 * divided by eight, rounded up; allocation and reclaim set bits.
 */
unavailable: Array<number>;  };

export type PrizePoolStateArgs = {
/**
 * Template authority that created the pool. Receives item rent when a
 * winner claims an item.
 */
authority: Address;
/** Bundle whose asset slot this pool fills; seeds this PDA. */
bundle: Address;
/** Nonzero Bubblegum Merkle tree that holds every deposited leaf. */
tree: Address;
/**
 * Chained hash of every deposited item in pool order. Removing the
 * unsealed tail restores the previous value; sealing commits it into the
 * bundle slot.
 */
manifestAccumulator: ReadonlyUint8Array;
/**
 * Items the pool must hold before sealing; equals the bundle quantity and
 * is between one and 4,096.
 */
quantity: number | bigint;
/**
 * Seal counter mixed into the bundle slot commitment; `sealPrizePool`
 * increments it from zero.
 */
version: number | bigint;
/** Items deposited so far, and the pool index of the next item. */
depositCursor: number;
/** Reserved items, including those already claimed by winners. */
assignedCount: number;
/** Assigned items already delivered to their winners. */
claimedCount: number;
/** Items returned to the creator from the sealed pool. */
reclaimedCount: number;
/** Bundle asset slot this pool fills; seeds this PDA. */
assetIndex: number;
/** 0 funding, 1 sealed. */
status: number;
/** One metadata-admitted item PDA exists at `deposit_cursor`. */
hasPreparedItem: boolean;
/** Canonical bump of this prize pool PDA. */
bump: number;
/**
 * A set bit means the item cannot be allocated again.
 * Holds one bit per deposited item, so its length is `deposit_cursor`
 * divided by eight, rounded up; allocation and reclaim set bits.
 */
unavailable: Array<number>;  };

/** Gets the encoder for {@link PrizePoolStateArgs} account data. */
export function getPrizePoolStateEncoder(): Encoder<PrizePoolStateArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()], ['migrationVersion', getU8Encoder()], ['authority', getAddressEncoder()], ['bundle', getAddressEncoder()], ['tree', getAddressEncoder()], ['manifestAccumulator', fixPinaPodEncoderSize(getBytesEncoder(), 32)], ['quantity', getU64Encoder()], ['version', getU64Encoder()], ['depositCursor', getU32Encoder()], ['assignedCount', getU32Encoder()], ['claimedCount', getU32Encoder()], ['reclaimedCount', getU32Encoder()], ['assetIndex', getU8Encoder()], ['status', getU8Encoder()], ['hasPreparedItem', getBooleanEncoder()], ['bump', getU8Encoder()], ['unavailable', getPinaPodBoundedArrayEncoder(getArrayEncoder(getU8Encoder(), { size: getU16Encoder() }), 512)]]), (value) => ({ ...value, discriminator: 8, migrationVersion: 0 }));
}

/** Gets the decoder for {@link PrizePoolState} account data. */
export function getPrizePoolStateDecoder(): Decoder<PrizePoolState> {
    return getStructDecoder([['discriminator', getPinaPodDiscriminatorDecoder(PRIZE_POOL_STATE_DISCRIMINATOR, getU8Decoder())], ['migrationVersion', getPinaPodMigrationVersionDecoder(0, getU8Decoder())], ['authority', getAddressDecoder()], ['bundle', getAddressDecoder()], ['tree', getAddressDecoder()], ['manifestAccumulator', fixDecoderSize(getBytesDecoder(), 32)], ['quantity', getU64Decoder()], ['version', getU64Decoder()], ['depositCursor', getU32Decoder()], ['assignedCount', getU32Decoder()], ['claimedCount', getU32Decoder()], ['reclaimedCount', getU32Decoder()], ['assetIndex', getU8Decoder()], ['status', getU8Decoder()], ['hasPreparedItem', getPinaPodBooleanDecoder()], ['bump', getU8Decoder()], ['unavailable', getPinaPodBoundedArrayDecoder(getArrayDecoder(getU8Decoder(), { size: getU16Decoder() }), getPinaPodBoundedCountDecoder(getU16Decoder() , 512), 512)]]);
}

/** Gets the codec for {@link PrizePoolState} account data. */
export function getPrizePoolStateCodec(): Codec<PrizePoolStateArgs, PrizePoolState> {
    return combineCodec(getPrizePoolStateEncoder(), getPrizePoolStateDecoder());
}

export function decodePrizePoolState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<PrizePoolState, TAddress>;
export function decodePrizePoolState<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<PrizePoolState, TAddress>;
export function decodePrizePoolState<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<PrizePoolState, TAddress> | MaybeAccount<PrizePoolState, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getPrizePoolStateDecoder());
}

export async function fetchPrizePoolState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PrizePoolState, TAddress>> {
  const maybeAccount = await fetchMaybePrizePoolState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePrizePoolState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PrizePoolState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePrizePoolState(maybeAccount);
}

export async function fetchAllPrizePoolState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PrizePoolState>[]> {
  const maybeAccounts = await fetchAllMaybePrizePoolState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePrizePoolState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PrizePoolState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePrizePoolState(maybeAccount));
}

export async function fetchPrizePoolStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PrizePoolSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<PrizePoolState>> {
  const maybeAccount = await fetchMaybePrizePoolStateFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePrizePoolStateFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PrizePoolSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<PrizePoolState>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findPrizePoolPda(seeds, { programAddress });
  return await fetchMaybePrizePoolState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const PRIZE_POOL_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `PrizePoolState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (prizePoolStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ prizePoolState: address, payer }).make());
 * }
 * ```
 */
export function prizePoolStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 8) {
		return false;
	}
	return data[1]! < 0;
}
